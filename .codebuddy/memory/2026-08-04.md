# 2026-08-04

## v0.1.13 发布修复（GitHub Releases）
- 发现 0.1.13 发布不完整：release v0.1.13 已创建（published 2026-08-04T01:31Z），但 **exe（80.26MB）上传失败**，仅有 blockmap(88KB)+latest.yml(359B)。根因同 0.1.11：80MB 大文件经 electron-builder 上传 GitHub 超时中断（日志停在 `overwrite published file ...blockmap`）。
- **修复**：用户提供 GH_TOKEN，用 curl 走 GitHub Uploads API 手动补传 exe：
  - `POST https://uploads.github.com/repos/jizihan2008/my-study-table/releases/{release_id}/assets?name=My-Study-Table-Setup-0.1.13.exe`，Header `Authorization: Bearer <token>` + `Content-Type: application/octet-stream`，`--data-binary @本地exe路径`
  - release id=364573021，HTTP 201，用时 57s（约 1.4MB/s，无需代理即可上传，但查询 API 同样可直连）。
  - 资产名须用连字符 `My-Study-Table-Setup-0.1.13.exe`（与 latest.yml 中 url 字段一致），不能用本地空格文件名。
- **验证**：release 资产齐全（exe 80.26MB + blockmap + latest.yml），下载 URL 跟随 302 后返回 200。electron-updater 更新链路已可用。
- 环境变量中无 GH_TOKEN/GITHUB_TOKEN、无 gh CLI、无 electron-builder token 缓存——每次补传需用户重新提供 token。token 明文未写入记忆。

## 插件系统 + AI 编程助手 v0.1.14 实现完成
- **版本**：package.json → 0.1.14，changelog id=15，README 新增第 16 章「扩展系统与 AI 编程助手」。
- **架构**：外部扩展统一存 `~/.my-study-table/extensions/<id>/`（manifest.json + main.js + backup/<时间戳>）。两类扩展：
  - `plugin` 安全插件：走受限 `window.extAPI` 白名单（registerNavItem / registerSection / addToolbarButton / on / emit / getData/setData/removeData / notify / log / callCore / openExternal），数据区独立为 `study_ext_<id>_*`。
  - `patch` 源码补丁：`PatchEngine.override/wrap/revertExt` 运行时函数覆盖，卸载恢复原函数，不动核心磁盘文件。
- **主进程 IPC（main.js 末尾新增）**：`ext:list` / `ext:read` / `ext:write` / `ext:backup` / `ext:list-backups` / `ext:restore` / `ext:remove` / `ext:open-dir` / `src:list` / `src:read`，全部做 `isPathInside` 白名单路径校验。preload.js 暴露对应 `window.electronAPI.extXxx / srcXxx`。
- **渲染进程**：`js/ext-api.js`（extAPI + ExtBus 事件总线）、`js/patch-engine.js`（PatchEngine）、`js/ext-manager.js`（ExtManager：init/mount/unmount/setEnabled/remove/addNavItem/addSection/addToolbarButton）、`js/codegen.js`（Codegen：buildContext/generate/parseGenerated/apply/rollback + renderCodegen 全局 UI）。加载顺序：ext-api → patch-engine → ext-manager → codegen（index.html updater.js 之后）。index.html 初始化 setTimeout 改为 async，先 `await ExtManager.init()` 再 renderSidebarNav。
- **UI**：侧边栏新增「AI 编程」（id=codegen, section-codegen）+ core.js ALL_NAV_ITEMS 加项、switchTab 加分支（动态 section 由 ExtManager 插入）。设置 tab 新增「扩展」（settingsPanelExtensions，含列表/开关/查看代码/回滚/卸载/打开目录/刷新）。
- **生命周期钩子**：扩展 main.js 定义 `window['<扩展id>_mount']`（注意 id 含连字符要用括号访问）。
- **测试**：临时 Electron smoke test（test_ext.js，已删）验证通过：ExtManager/extAPI/PatchEngine/Codegen 加载、设置面板渲染、codegen 渲染、插件装载（动态 nav/section/生命周期/事件总线）、禁用/启用/卸载清理、补丁覆盖与恢复、0 渲染错误。示例插件/补丁代码在 smoke test 中，未保留。
- **经验**：extAPI.notify/openExternal 的 electronAPI invoke 返回 promise 异步 reject 需 `.catch()` 吞掉，否则产生未捕获 rejection。

## UI 修复与 AI 编程界面重设计
- **扩展 tab 看不到的根因**：`.settings-tabs` 的 `.settings-tab { flex:1 }` 在 520px 宽的设置 modal 中，9 个 tab 放不下导致第 9 个「扩展」tab 被挤压溢出。修复：`.settings-tabs` 加 `flex-wrap: wrap`，`.settings-tab` 改 `flex: 0 1 auto; min-width: 76px`，tab 自动换行全部可见。
- **AI 编程界面重设计**（js/codegen.js UI 部分 + css 样式）：旧单栏布局改为：
  - `.cg-topbar` API 状态条（绿点「API 已连接」/ 红点「未配置 API Key」+ 模型信息 + 「管理 API」按钮，openSettingsApiTab 跳设置 API tab）
  - `.cg-input-card` 输入卡片：`.cg-input-title` + textarea + 渐变紫 `.cg-gen-btn` 大按钮 + `.cg-samples` 5 个示例需求 chips（fillCgSample 点击填充）+ `.cg-type-hint`（插件/补丁类型提示）
  - `.cg-guide` 空态指引：3 张能力卡（新增功能/修改现有功能/安全可回滚）
  - `.cg-progress` 生成进度动画（3 步 + 流动进度条）
  - `.cg-result` 结果卡片：头部（类型徽章 + id + 复制代码 copyCgCode）、左右分栏 `.cg-result-split`（左功能说明用 `formatMarkdownBase` 渲染 markdown，右深色 `.cg-code` 代码预览）、底部「应用扩展（自动备份）」+「放弃」
  - `.cg-success` 应用成功横幅（party-popper 图标 + 打开扩展管理 + 继续生成）
- **新增全局函数**：fillCgSample / copyCgCode / openSettingsApiTab / openExtensionsSettings / cgContinue（window 导出）
- `.cg-status` 增加 `:not(:empty)` 规则显示成功消息（原默认 display:none 只显示错误态）
- 响应式：`.cg-guide`/`.cg-result-split` 小屏单列，输入行小屏纵向。

## 扩展 tab 独立成页面 + 设置 tab 横向滑动
- **扩展独立页面**：设置弹窗中的「扩展」tab 及其面板已移除（settingsTabExtensions/settingsPanelExtensions 删除），改为独立侧边栏页面 `#section-extensions`（section-header + `.ext-toolbar-row` 操作栏：用 AI 生成扩展/打开目录/刷新 + `#extensionsList` + `#extensionsStatus` + `.ext-storage-hint` 存储提示）。core.js `ALL_NAV_ITEMS` 加 `{ id:'extensions', icon:'puzzle', label:'扩展' }`，switchTab 加 `if (tab==='extensions') renderExtensionsPanel()`。renderExtensionsPanel 渲染目标不变（#extensionsList/#extensionsStatus），无需改逻辑。
- **设置 tab 横向滑动**：`.settings-tabs` 从 `flex-wrap:wrap` 改回 `flex-wrap:nowrap; overflow-x:auto; overflow-y:hidden` + 4px 细滚动条（`::-webkit-scrollbar { height:4px }`），`.settings-tab` 改 `flex:0 0 auto; min-width:76px; padding:10px 14px`，单行可左右滑动。
- **引用同步**：codegen.js `openExtensionsSettings()` 从 `openSettingsModal()+switchSettingsTab('extensions')` 改为直接 `switchTab('extensions')`；README 及 codegen 文案中「设置 → 扩展」改为「扩展」页面。
- 验证：smoke test 通过——section-extensions/nav-extensions 存在、设置弹窗无扩展 tab、tabsOverflowX=auto + nowrap、扩展列表正常渲染 demo 卡片、0 渲染错误。

## 三个功能内置插件化 + 设置 tab 修复
- **设置 tab 看不到的修复**：520px modal 太窄，8 个 tab 放不下。`#settingsModal .modal { max-width: 720px }` + `#settingsModal .settings-tab { min-width:60px; padding:10px 10px; font-size:12px }` + 设置 modal 内隐藏滚动条，8 个 tab 全部可见（smoke test 验证 allSettingsTabsVisible=true）。
- **内置扩展（自带插件）**：快捷访问 / 音乐播放器 / 学习统计 三个功能插件化。
  - 新文件 `js/builtin-extensions.js`：`window.BUILTIN_EXTENSIONS` 数组，定义 `builtin-links` / `builtin-music` / `builtin-stats`，main 代码用 extAPI.registerNavItem + registerSection（html 复用原 section 结构，render 回调调 renderLinks/renderMusic/renderStats）。
  - `js/ext-manager.js`：loadAll 合并内置扩展（`loadBuiltinState` 读 localStorage `study_ext_builtin_<id>`，值为 enabled/disabled/removed）；setEnabled/remove 对 builtin 走 localStorage 而非 manifest/IPC；新增 `restoreBuiltin(id)` 与 `listBuiltins()`。
  - `js/settings.js`：renderExtensionsPanel 改用 `ExtManager.list()`（替代 electronAPI.extList）以包含内置扩展；内置徽章 `.ext-badge-builtin`；内置扩展隐藏回滚按钮、卸载按钮文字改「移除」；底部「已移除的内置扩展」恢复区 + `restoreBuiltinExt()`；viewExtCode 对内置扩展从 registry 读 mainCode。
  - `index.html`：移除 section-links/music/stats 的 HTML；加载 `builtin-extensions.js`（在 ext-manager.js 之前）。
  - `js/core.js`：ALL_NAV_ITEMS 移除 links/music/stats；switchTab 移除 music/stats 分支（由扩展 render 回调负责）。
  - `js/utils.js`：safeInit 移除 `renderLinks`/`onLinkTypeChange`/`renderMusic`（扩展装载晚于脚本加载，提前渲染会因 #linksPanels 等为 null 报错）。
- **关键坑**：内置扩展 main 是模板字符串，其中嵌入的 JS 代码含单引号字符串（如 `'Enter'`）会导致 new Function 解析失败——模板字符串中 `\'` 会被解析为 `'`（反斜杠丢失），必须写 `\\'` 双层转义，或改用 `String.fromCharCode(13)` 避免单引号。
- 验证：smoke test 全通过——3 个内置扩展装载、nav-links/music/stats 出现、三个 section 正常渲染、扩展页 3 卡片 + 3 内置徽章、禁用/启用正常、设置 tab 全可见、0 渲染错误。

## v0.1.15：AI 编程助手升级为 CodeBuddy CLI Agent 模式
- **用户需求**：担心 DeepSeek 等 API 全栈能力弱，要支持 CodeBuddy 这种完整 Agent；本机无 CLI 时可一键安装。
- **调研确认**：官方包名 `@tencent-ai/codebuddy-code`（命令 codebuddy/cbc，Node≥18.20）；`-p/--print` 非交互；`--output-format stream-json`；`-p` 写文件必须 `--dangerously-skip-permissions`；`--allowedTools`/`--add-dir` 控制边界；授权三方式（OAuth 登录 / `CODEBUDDY_API_KEY`+`CODEBUDDY_INTERNET_ENVIRONMENT=internal` / 企业 OAuth2）。Agent SDK `@tencent-ai/agent-sdk` 为 Preview 不引入。
- **main.js**：新增 `codebuddy:locate`（用户配置 `study_codebuddy_cli_path` 优先→npm 全局 bin 探测 codebuddy.cmd/cbc.cmd→常见目录）、`codebuddy:install`（检测 npm→`npm install -g @tencent-ai/codebuddy-code`，可选 npmmirror 镜像，流式回传 `codebuddy:install-output`→重探测）、`codebuddy:run`（spawn：cwd=extensionsDir + `-p <prompt> --output-format stream-json --add-dir <sourceDir> --allowedTools "Read Edit Write" --dangerously-skip-permissions`，sourceDir=开发模式`__dirname`/打包模式 source-snapshot；apiKey 注入 env；stdout/stderr 逐行 `codegen:agent-output` 流式回传）、`src:export-snapshot`（js/css/lib/index.html/main.js/preload.js/package.json 导出到 `~/.my-study-table/source-snapshot/`）。spawn 参数全数组传防注入；引入 `child_process.spawn`。
- **preload.js**：暴露 codebuddyLocate/Install/Run、srcExportSnapshot、onCodegenAgentOutput、onCodebuddyInstallOutput。
- **js/codegen.js 全量重写**：移除 `callAiApi` 直调；新增 `buildAgentPrompt`（架构说明+manifest/extAPI/PatchEngine 规范+权限约束"只写 extensions/ 源码只读"+输出约定一行 JSON 摘要 `{type,id,name,description,summary}`）、`runAgent`（预备份全部扩展→codebuddyRun→流式追加 `.cg-agent-log`→parseAgentSummary→ExtManager.loadAll+init 装载）、`parseAgentSummary`（从 stdout 尾部找含 type+id 的 JSON 行，兼容 stream-json 包裹）、`locateCli`（带缓存+force）、`getCodebuddyApiKey/setCodebuddyApiKey`（localStorage `study_codebuddy_api_key`）。UI 顶部改 CLI 状态徽章（refreshCliBar），新增 `.cg-agent-log` 日志区、一键安装入口 `installCliFromCodegen`（复用 install IPC + 安装日志）。保留 apply/rollback/parseGenerated 兼容旧路径。renderAgentResult 展示摘要；无摘要时降级提示"已写入扩展目录，到扩展页查看"。
- **js/settings.js**：新增 CodeBuddy CLI 配置区渲染 `renderCodebuddyCliConfig`（探测状态卡、路径输入存 `study_codebuddy_cli_path`、重新探测、一键安装+国内镜像勾选、API Key 输入、未安装安装指引、安装日志区）；配套 saveCodebuddyCliPath/reprobeCodebuddyCli/installCodebuddyCli/saveCodebuddyApiKey/clearCodebuddyApiKey。`switchSettingsTab('api')` 分支触发（含 hook 版）。changelog id=16（v0.1.15）。
- **css/style.css**：`.cg-agent-log`（等宽字体、深色底、自动滚动）、`.cg-key-status.installing/offline` 态、日志行颜色类（.log-info/.log-success/.log-error/.log-tool）。
- **index.html**：AI 设置面板新增 `#codebuddyCliConfig` 配置区。
- **版本**：package.json 0.1.15，README 第 16 章 AI 编程助手改写为 CLI 模式说明。
- **验证**：全量 node --check + 0 lint；mock spawn smoke test 通过——locate 探测、buildAgentPrompt 含规范/权限约束/需求、parseAgentSummary、renderCodegen 完整 UI、cgGenerate→runAgent→流式日志显示→result 卡片含扩展名、设置页配置区完整（路径/安装按钮/APIKey/镜像/安装指引）、AI 编程页顶部未安装提示、0 渲染错误。
- **真实使用**：需本机安装 `@tencent-ai/codebuddy-code` 并登录（终端 codebuddy）或配置 API Key 后手动验证。注意 `--dangerously-skip-permissions` 是 CLI `-p` 模式写文件硬性要求。

## 修复：扩展启用/禁用不刷新 + 设置 tab 被遮挡
- **启用/禁用不刷新根因**：`ExtManager.setEnabled` 对内置扩展只 `saveBuiltinState`（localStorage），未同步 `ext.meta.enabled`，而 `renderExtensionsPanel` 用 `meta.enabled` 渲染开关 → 刷新后 UI 状态不变。修复：builtin 分支同时更新 `ext.meta.enabled = !!enabled`。
- **设置 tab 被遮挡根因（两次排查）**：
  1. 用 elementFromPoint + rect 几何测量发现 `.settings-tabs` 容器高度异常（13px）且 tab 按钮被压缩——modal 是 `flex column` + `max-height:70vh`，`.settings-tabs` 默认 `flex-shrink:1`，内容超高时被 flex 压缩裁剪。
  2. 修复：`.settings-tabs` 加 `flex-shrink: 0`；`.settings-tab` 改 `display:inline-flex; align-items:center; justify-content:center; gap:6px; line-height:1.4`（图标文字垂直居中）；`#settingsModal .settings-tab` 加 `min-height:38px; padding:8px 10px`。
  - 注：测试环境无网络时 lucide CDN（unpkg.com/lucide@latest）加载失败，`<i data-lucide>` 图标不替换，tab 高度假性变小——真实应用在线则图标正常，但 flex-shrink 修复对两种场景都有效。
- 验证：8 个设置 tab 全部可见（高 36px）、header 与 tabs 无重叠、内置扩展开关禁用后 checkbox=false + 导航消失、启用后恢复、0 渲染错误。遗留 `.test-extensions` 测试目录（用户拒绝删除，保留）。

## 编辑界面栏支持动态扩展项
- 插件/内置扩展注册的动态侧边栏项（links/music/stats）现在可在「编辑界面栏」（sidebar 齿轮按钮 → openNavSettings）中**拖拽排序、显示/隐藏、设为启动首页**。
- 改动（js/core.js）：
  1. 新增 `getNavDisplayItems()`：返回 ALL_NAV_ITEMS + `ExtManager.getDynamicNavItems()` 合并列表。
  2. `loadNavConfig()` 重写：始终合并动态扩展 id 到 order（首次无 saved 时 order=allIds，且保留已卸载扩展之外的全部），hidden 数组兜底。
  3. `renderSidebarNav()`：mergedOrder 改为 cfg.order 去重合并 dynamicIds（防止动态 id 已入 order 后重复渲染）；动态项改为**尊重 hidden**（原 `isHidden && !dyn` 逻辑忽略动态项隐藏）。
  4. `openNavSettings()` homeOptions 改用 getNavDisplayItems()；`renderNavSortList()` 用 getNavDisplayItems() 匹配 id。
- 验证：smoke test——编辑界面栏 14 项（11 核心 + 3 动态）、homeSelect 含动态项、隐藏 links 后侧边栏消失、重新显示恢复、核心项不受影响、0 渲染错误。

## 修复：编辑界面栏勾选后侧边栏不刷新
- **根因**：`toggleNavItemVisibility`（core.js）只调 `renderNavSortList(cfg)` 重绘排序列表，**未调用 `renderSidebarNav()`**，导致勾选显示/隐藏后侧边栏不即时刷新。修复：在函数末尾追加 `renderSidebarNav()`。
- **增强**：`ExtManager.insertSectionDOM` 插入扩展页面后立即 `lucide.createIcons()`，确保扩展页面内图标即时渲染（此前依赖后续 switchTab 才渲染）。
- **快捷访问 UI 排查**：通过 DOM 检查确认内置扩展的快捷访问页面结构/交互正常（新增按钮可见、input-panel 用 max-height 收起默认折叠、点击新增展开、lucide 图标渲染 3 个、空态存在、0 渲染错误）。`toggleLinkInput` 定义在 js/todos.js（历史遗留，非 links.js）。快捷访问数据键 `study_links_v3`（core.js `let links = loadData('study_links_v3')`，非 `study_links`）。用户报告的具体 UI 问题待用户描述（自动化测试未复现明确 bug）。
- 验证：click checkbox 后 nav-links 立即消失/恢复（afterClick_navGone=true, afterClick2_navBack=true）、0 渲染错误。

## 修复：快捷访问 UI 布局错乱（内置扩展 HTML 结构 bug）
- **根因**：`js/builtin-extensions.js` 的 builtin-links 扩展 html 数组中，`.link-inputs` 内**多了一个重复且未闭合的 `<div class="input-row">`**（第 37-38 行重复），导致 DOM 结构错乱：输入区被错误嵌套、链接列表 `#linksPanels` 被挤入 `#linkInputPanel`（overflow:hidden + max-height:0 收起）中隐藏，表现为"已有快捷访问的显示消失、新增 UI 挤成一行"。
- **修复**：删除多余的 `<div class="input-row">` 行。
- **验证**（DOM 检查）：`.link-inputs .input-row` 数量=3（名称/类型+URL/分类+添加，每行元素数 1/2/2）、`#linksPanels` 不在 `#linkInputPanel` 内且位于 `.card-scroll`、点击「新增」后 `.open` 生效（max-height 400px + opacity 1）、三行垂直排列（rowTops 间距 50px）、空态显示正常、0 渲染错误。注意 headless 测试中 CSS transition 不执行导致 getBoundingClientRect 高度测量不准，应改用 computed style（maxHeight/opacity）验证展开状态。

## AI 编程 UI 改造成「多项目标签页 + 消息流」（仿 AI 助手）
- **需求**：把 AI 编程页 UI 改成与 AI 助手一致的布局，不同标签页对应不同「项目」，不需要树形结构。
- **index.html**：`#section-codegen` 的 card 改 `padding:0; overflow:hidden`（仿 section-ai），去掉了 section-header；`#codegenBody` 加 `cg-layout` 类；codegen.js 版本 bump 到 `v=20260804-r2`。
- **js/codegen.js UI 全量重构**（核心运行逻辑 buildAgentPrompt/runAgent/locateCli/parseAgentSummary/apply/rollback 不变）：
  - 数据模型：localStorage `study_cg_projects`（项目数组）+ `study_active_cg_project`；项目 = `{id,title,createdAt,messages,draft}`；消息 = `{role:'user'|'assistant',content,time,running,ok,exitCode,summary,log,error,install,_uid}`，**线性消息流，无树**。
  - `loadCgProjects` 只在 `cgProjects.length===0`（首次）时从 localStorage 加载——**切勿每次 renderCodegen 重载**，否则运行中消息对象引用脱离数组导致流式日志丢失（踩坑修复）。
  - 标签条：`.cg-conv-tabs-wrap`，支持「＋」新建（createNewCgProject）、点击切换（switchCgProject）、✕ 删除（deleteCgProject，只剩一个时转清空）、垃圾桶清空（clearCgProject）、**双击标签内联重命名**（startCgRename/commitCgRename/handleCgRenameKey）。`_generating` 时禁止新建。
  - 消息流 `.cg-messages`：user 需求气泡（右侧主色）+ assistant 结果卡（复用 `.cg-result`：运行中=实时日志 pre `cgLog-<uid>` 流式追加；成功=摘要卡 + `<details>` 折叠日志；失败=错误卡 + 日志；安装 CLI 消息走 `install:true` 同流显示）。
  - 输入区 `.cg-input-wrap` 在底部：textarea（Enter 发送，handleCgInputKey）+ 运行按钮 + 示例 chips + 类型提示；每个项目草稿独立保存（draft）。
  - 切换标签保存当前草稿；运行中可切标签，后台继续流式累积日志，切回恢复显示。
  - `loadCgProjects` 里清理上次会话遗留的 `running` 消息（标为「上次运行已中断」），避免重启后永久卡运行中。
  - cgContinue/cgDiscard 保留为兼容入口（聚焦输入框）；installCliFromCodegen 改为把安装日志写入当前项目消息流。
- **css/style.css** 新增：`.cg-layout`、`.cg-conv-tabs-wrap/.cg-conv-tab/.cg-conv-tab-add/.cg-conv-tab-delete/.cg-conv-tab-clear/.cg-conv-tab-name/.cg-rename-input`、`.cg-messages/.cg-msg/.cg-msg-avatar/.cg-msg-bubble/.cg-user-bubble/.cg-running-label/.cg-msg-time`、`.cg-log-box/.cg-log-toggle`（details 折叠日志）、`.cg-result-error`、`.cg-input-wrap`、`.cg-guide-tip`；`.cg-layout .cg-topbar` 压扁为消息区上方细条（去圆角/上下边框）。
- **验证**：node --check 通过、0 lint；`showCustomConfirm`（ai-utils.js）/`formatMarkdownBase`（ai-render.js）/`fadeSlideIn` keyframes/`openCodegenSection`（settings.js）等依赖确认存在。需完全重启 Electron 生效（改了 js/css/index.html）。
- **踩坑**：用户反馈"有标签页但不是消息流布局"——根因是 **index.html 的 section-codegen 修改丢失**（此前 replace 成功但被外部操作回滚为旧结构：仍有 section-header + card 默认 padding，且 `#codegenBody` 丢了 `cg-layout` 类）。无 `cg-layout` 类时 flex 布局失效，消息区塌陷成内容高度，页面就变成「标签条+小区域+输入框」堆叠。已重新应用修改并确认保存。教训：改动 index.html 后应 read_file 回读确认持久化，避免外部覆盖。
- 待办：真实环境重启验证多标签交互与流式日志。

## 修复：CodeBuddy CLI 输出乱码 + agent 异常退出 code=1（main.js）
- **现象**：启动 agent 时日志乱码「������̫����」，随后 agent code=1 退出。
- **根因（两个）**：
  1. Windows 下 `spawn(codebuddy.cmd, args, { shell: true })` 走 cmd.exe 二次解析，长 prompt 中的双引号（JSON 摘要）和 `--allowedTools Read Edit Write` 等含空格参数被破坏 → CLI 参数错乱报错退出 code=1。
  2. `String(d)` 强制按 UTF-8 解码子进程输出，Windows 下 GBK 内容全变 U+FFFD 乱码，真实错误信息不可读。
- **修复（main.js）**：
  1. 新增 `resolveCliEntry(cliPath)`：读取 .cmd/.bat 包装脚本内容，正则提取真实 JS 入口路径（`%dp0%\node_modules\@tencent-ai\codebuddy-code\bin\xxx.js`），返回 `{script}`；失败返回 null。
  2. `codebuddy:run` 优先 `spawn(process.execPath, [script, ...args], { env: {..., ELECTRON_RUN_AS_NODE:'1'} })` —— **Electron 的 process.execPath 是 electron.exe，必须加 `ELECTRON_RUN_AS_NODE=1` 才会以纯 Node 模式执行脚本**（否则会启动 Electron 窗口）。绕开 cmd.exe 后：无参数转义问题、输出纯 UTF-8。
  3. 回退方案：`cmd.exe /d /s /c <quoteCmdArgs>`（参数逐个双引号包裹 + `"`→`\"`）。
  4. 新增 `createStreamDecoder()`：**首段缓冲 1KB 探测编码**——用 `TextDecoder('utf-8',{fatal:true})` 严格解码去掉末尾 3 字节的探针，失败则整体切 `TextDecoder('gb18030')`；探测后必须 `pending = Buffer.alloc(0)` 清空（否则 flush 重复解码前 1KB）；`flush()` 兜底。
  5. `codebuddy:install` 的 npm 输出同样套 decoder。
- **渲染端**：codegen.js 失败卡片新增 `cgAuthHint`——错误含 login/auth/api key/未登录/未授权/登录/授权/token 时提示「终端运行 codebuddy 登录，或到设置→AI 设置配置 API Key」。版本 bump 到 r3。
- **验证**：单元测试 6/6 通过（utf8 短/长文本、GBK 自动切换无乱码、UTF-8 分块边界无损、.cmd 解析、非 cmd 返回 null），测试脚本已删；main.js/codegen.js `node --check` + 0 lint。
- **经验**：GBK 编码的"失败"=`CA A7 B0 DC`（此前误记为 C6F4 B4ED 那是"启错"）。Electron 内置 Node 为 full ICU，TextDecoder 支持 gb18030；本机 node 也支持。
- **二次修复（真正的根因）**：修复后仍乱码 + code=1。在本机实测 `C:\Users\纪子涵\AppData\Roaming\npm\codebuddy.cmd`：**npm 全局 bin 入口是 `node_modules\@tencent-ai\codebuddy-code\bin\codebuddy`（无 .js 扩展名，shebang `#!/usr/bin/env node` 的 JS 脚本）**，而 `resolveCliEntry` 旧正则要求 `.js` 结尾 → 解析失败返回 null → 走了 cmd.exe 回退分支 → 长 prompt 引号被 cmd 破坏 → code=1 + GBK 乱码。修复：`resolveCliEntry` 改为提取 .cmd 中所有双引号包裹路径，从后往前找含 `node_modules` 且以 `[\\/]bin[\\/]<name>` 或 `.js/.mjs/.cjs` 结尾的入口，替换 `%dp0%` 并校验存在。
- **实测验证（本机）**：修复后解析真实 codebuddy.cmd → 正确得 `...\bin\codebuddy`；用项目 `node_modules\electron\dist\electron.exe` + `ELECTRON_RUN_AS_NODE=1` 完整跑 `-p hello --output-format stream-json --add-dir <项目根> --allowedTools "Read Edit Write" --dangerously-skip-permissions`，**exit=0，stream-json 正常输出，无乱码**，初始化消息显示 `apiKeySource: www.codebuddy.ai`（用户 CLI 已登录）、`permissionMode: bypassPermissions`。注意：npm 包名/路径含非 ASCII（纪子涵）不影响 spawn；**cwd 无效时 spawn 报 ENOENT 且错误信息指向 exe 路径，易误判**（应先确认 cwd 存在）。临时测试脚本已删。
- 待办：用户完全重启应用后重试真实需求，确认乱码消失、agent 正常执行写扩展。

## js/codegen.js 文件丢失与重建
- **现象**：用户重启后报 `codegen.js Failed to load resource: net::ERR_FILE_NOT_FOUND`。排查确认 **`js/codegen.js` 从磁盘消失**（search_file 与 list_dir 均无，index.html 引用 `v=20260804-r3` 正常）。
- **原因不明**：此前对 codegen.js 的多轮 replace 均返回成功，git 不可用（不在 PATH），无法确认是否被外部进程/杀毒/同步删除或用户操作。
- **处理**：根据对话历史中的完整最新内容重建 `js/codegen.js`（多项目标签页 UI + 项目数据管理 + 运行流程 + `cgAuthHint` 失败提示），`node --check` + 0 lint 通过。
- **教训**：重要文件修改后应回读确认持久化；若用户环境有同步/杀毒软件可能拦截 .js 写入，需关注。当前 index.html 引用 `js/codegen.js?v=20260804-r3`。

## agent 误判成功 + 假扩展卡片修复（codegen.js r4）
- **现象**：用户需求运行后 agent 日志末尾 `"errors":["Authentication required. Please use /login command to sign in to your account"]`（**CLI 实际未登录**，apiKeySource 仅配置痕迹），但界面却显示了一个"安全插件 cf0829ee-..."结果卡片，扩展目录实际为空。
- **误判根因**：agent 失败时没输出约定摘要，`parseAgentSummary` 从 stream-json 里误抓了 `{"type":"file-history-snapshot","id":"cf0829ee-..."}`（恰好有 type+id）当作摘要。
- **修复（codegen.js）**：
  1. `parseAgentSummary` 严格化：type 必须 ∈ {plugin, patch} 且含 id+name，排除系统事件。
  2. 新增 `extractAgentError(stdout)`：从 stream-json 的 `{"type":"result","is_error":true,"errors":[...]}` 提取失败原因——**CLI 退出码 0 但 agent 内部失败时必须据此判失败**。
  3. `runAgent`：`ok = result.ok && !hardError`；仅 ok 时解析摘要、仅 ok 时 `ExtManager.loadAll/init`；`stderr` 返回 hardError。
  4. 日志可读化：新增 `formatAgentLogLine`（stream-json 行 → 人类可读：init→"✔ Agent 已连接（登录来源）"、assistant 文本、tool_use→"🔧 调用工具 xxx"、result→"✔ 完成/✘ 出错"、隐藏 system/file-history-snapshot/progress 噪音）+ `createLogAppender`（**按行缓冲**，长 JSON 行被拆 chunk 时先拼完整行再格式化）。
  5. 版本 bump r4。
- **验证**：单元测试 9/9（file-history-snapshot 不误判、真实摘要识别、is_error 检测、无失败返回空、init/assistant/result 格式化、分块缓冲正确）。
- **用户待办**：CLI 未登录是真正的阻塞项——需在终端 `codebuddy` 交互登录（/login）或配置 API Key 后 agent 才能工作；修复后未登录时界面会显示真实错误而非假成功。

## 新增：CLI 登录自动检测 + 未登录引导弹窗（codegen.js r5）
- **需求**：自动检测用户是否登录 CodeBuddy；未登录时弹窗，提供「打开终端登录」「跳转配置 API Key」两个选项。
- **检测方案（main.js `codebuddy:check-login`）**：CLI 无非交互 login/status 子命令，登录凭据也不在 `~/.codebuddy` 明文文件（user-state.json 无凭据）→ 采用**跑极简 `-p hi` 任务检测**：未登录时 CLI 立即输出 `Authentication required. Please use /login`（正则匹配→loggedIn=false）；已登录时任务正常推进，12s 超时即 kill 并判定已登录。参数 `-p hi --output-format stream-json --add-dir extensionsDir --allowedTools Read --dangerously-skip-permissions`。
- **打开终端（main.js `codebuddy:open-login-terminal`）**：Windows `spawn('cmd.exe',['/c','start "" cmd /k "<exe>"'],{detached:true,stdio:'ignore'}).unref()` 打开保持窗口的终端运行 codebuddy；macOS `open -a Terminal`；Linux `x-terminal-emulator -e`。
- **渲染端（codegen.js r5）**：
  - `ensureLoginCheck(force)`：登录检测带 **5 分钟缓存 + 并发共享同一 Promise**（防重复跑检测）。
  - `cgLoginUsable(login)`：**已配置 API Key（`study_codebuddy_api_key` 非空）即视为可用**，不再因 CLI OAuth 未登录而误弹窗。
  - `showCgLoginModal()` 动态创建模态（复用 `.modal-overlay/.modal` 类，id=`cgLoginModal`，z-index 300）：两个大按钮「打开终端登录」(primary)「配置 API Key」+「已登录？重新检测」；`_loginModalShown` 保证本会话只弹一次。
  - `refreshCliBar` 三态：已连接(绿)/未登录(黄，含"去登录"按钮)/未安装(红)。
  - `renderCodegen` 末尾异步 `ensureLoginCheck().then(...)` 未登录则弹窗。
  - 全局暴露 showCgLoginModal/closeCgLoginModal/openCgLoginTerminal/openCgApiKeyConfig/recheckCgLogin。
- **CSS**：`.cg-login-overlay/.cg-login-actions/.cg-login-btn(.primary)/.cg-login-btn-desc/.cg-login-recheck`。
- **实测**：本机跑 `-p hi` 检测 → 正确识别当前未登录（auth-required → loggedIn=false）。index.html bump `js/codegen.js?v=20260804-r5`；main.js/preload.js/codegen.js `node --check` + 0 lint 通过。

## 修复：打开登录终端报"Windows 找不到 'lI' 文件"
- **根因**：原实现 `start "" cmd /k "C:\Users\纪子涵\...\codebuddy.cmd"` 把完整中文路径直接拼进 cmd 命令行，cmd 用系统代码页(GBK)解析，非 ASCII 路径被破坏成乱码（`'lI'` 即乱码残片）→ 找不到文件。
- **修复**：改为 `spawn('cmd.exe', ['/d', '/k', 命令名])`——命令名取 `path.basename(cliPath).replace(/\.(cmd|bat|exe)$/i,'')`（仅当纯 ASCII 时用，否则默认 `codebuddy`），让 cmd 按 PATHEXT 在 PATH 中自行查找 `.cmd`，彻底绕开中文路径。`/d` 禁 AutoRun、`/k` 保持窗口。stdio:'ignore' + windowsHide:false + detached + unref。
- **验证**：`where codebuddy` 确认命令在 PATH（`C:\Users\纪子涵\AppData\Roaming\npm\codebuddy.cmd`）；`cmd /d /c codebuddy --version` → 输出 2.132.0、exit 0。
- **经验**：Windows 下 spawn cmd 传含非 ASCII 的完整路径极易因代码页损坏；优先用 PATH 命令名让 cmd 自查。

## 「重新检测」入口常驻顶部状态条（codegen.js r6）
- **背景**：用户问「已登录？重新检测」在哪里——该按钮原本只在登录引导弹窗底部，弹窗不显示时（已判定可用/已关闭）没有常驻入口。
- **改动**：顶部 CLI 状态条右侧新增常驻「重新检测」按钮（`recheckCgLogin()`，刷新图标），与「CLI 设置」并列（`.cg-topbar-right` flex 容器），任何状态都可见。index.html bump `js/codegen.js?v=20260804-r6`。

## Agent 动态改为「一次回复 = 一个大气泡」流式展示（codegen.js r7 + style.css）
- **需求**：不要把所有日志堆在一个 `<pre>` 里，也不要每条 agent 输出独立小气泡；而是要「一次回复(reply)为一个大气泡，收到一条插一条」，对齐 CodeBuddy 的展示。
- **数据模型**：消息新增 `logGroups`（数组），每个元素 = 一次 assistant 事件（一次回复）→ `{ parts:[...] }`，同一事件内的 text + 多个 tool_use 归入同一组。`getCgLogGroups(m)` 优先 logGroups，兼容旧 `logItems`（包成一个组）与纯文本 `log`（`splitLegacyLogGroups` 按工具调用/状态段切组）。
- **解析**：`parseAgentLogLine` 由"条目"改为返回一个"回复组" `{kind, parts}`：assistant 事件返回含全部 content parts 的一组；system.init/result/错误各成一个独立小组；file-history-snapshot/progress 噪音忽略。`formatAgentLogLine` 复用它拼纯文本。
- **渲染**：新增 `renderCgLogToggleStream(m)`（完成态：`<details open>` + `.cg-log-stream` 内逐组气泡）、`renderCgLogGroups`/`renderCgLogGroupHtml`（组外框按 `groupKind` 配色 error>ok>sys>tool>text）、`renderCgLogPartHtml`（组内 part：tool 行=图标+名称+截断 input 的 code；text=pre-wrap；状态行）。运行中分支从 `<pre id="cgLog-">` 改为 `<div class="cg-log-stream" id="cgLogStream-">` + `renderCgLogGroups(m)`。
- **流式**：`createLogAppender` 按行缓冲解析出 group 后 `msg.logGroups.push(r)` + `appendCgLogGroupEl`（DOM 增量插一个大气泡，lucide 图标即时渲染）。
- **CSS**：新增 `.cg-log-stream`（flex 列 + gap + 深色底 + 滚动）、`.cg-log-group`（气泡卡，`animation: cgLogIn` 淡入，sys/ok/error/tool 配色）、`.cg-log-part*`（tool 行紫、text 白、ok 绿、error 红、sys 灰）。
- **遗留**：install CLI 消息仍走 `renderCgLogToggle` 纯文本折叠 + `cgLog-`（未改）；`cgLog-` 引用仅剩 install 场景。
- **验证**：node --check + 0 lint。需完全重启 Electron 生效。
- **修复（r7 后补丁）**：实测报两个 ReferenceError——(1) running 分支误调 `renderCgLogStreamItems(m)`（函数已改名 `renderCgLogGroups`），已改正；(2) `autoResizeCgInput` 定义在 IIFE 内但未导出，HTML 内联 `oninput="autoResizeCgInput()"` 找不到全局函数，已在 window 导出列表补 `window.autoResizeCgInput = autoResizeCgInput`。教训：codegen.js 内部函数若被 index.html 内联 onclick/oninput 引用，必须显式挂到 window。

## Agent 动态展示大升级（codegen.js r8 + main.js + style.css）
用户跑 today-quote-card 扩展后提了 5 项优化，全部完成：
1. **init 大 JSON 折叠**：根因是 main.js `codebuddy:run` 的 meta 文本（`启动 CodeBuddy CLI agent …`）无换行符，渲染端 `createLogAppender` 按 `\n` 行缓冲时与紧随的 3KB init JSON 拼成同一行 → 整行当纯文本显示。修复：(a) main.js 两处 meta text 补 `\n`；(b) `parseAgentLogLine` 容错——行首非 `{` 时尝试 `indexOf('{"type":')` 提取 JSON 事件单独解析，前置文本与事件拆成多组（`{kind:'multi', _groups}`），createLogAppender/formatAgentLogLine 展开 `_groups` 逐个入组。
2. **工具名中文化**：新增 `cgToolLabel(name)` 映射（Bash/PowerShell→终端命令、Read→读取文件、Write/Edit→写入/编辑文件、Grep→代码搜索、Glob/ListDir/SearchFile→查找文件、WebFetch/WebSearch→联网检索、Task→子任务、Skill→使用技能、LSP→代码检查、Notebook→笔记编辑、Agent→子代理、Image/Video→生成媒体）。
3. **工具详情撑满宽度 + 可展开**：`parseAgentLogLine` 不再把 tool input 截断到 80（改存完整 JSON），渲染时 `<code class="cg-tool-input" data-full="完整参数">` 显示截断（`truncateCgInput` 120 字），点击 `toggleCgLogToolDetail(this)`（window 全局）切换 `cg-tool-open` 类展开完整内容。CSS：`flex:1 1 auto; min-width:0; white-space:nowrap; ellipsis`，展开态 pre-wrap + break-all + max-height 240px 滚动。
4. **AI 文本 markdown 渲染**：text part 改用 `renderMarkdownSafe()`（复用 ai-render.js 的 formatMarkdownBase，含代码块/表格/标题/列表/LaTeX）。CSS 新增 `.cg-log-part-text` 内 p/h1-h4/ul/ol/pre/code/a/hr/table/blockquote 样式。
5. **完成后折叠 + 显示最后回复 + 插件详情**：`renderCgLogToggleStream` 的 `<details>` 去掉 `open`（默认折叠），summary 显示「最后回复：<最后一条纯文本前60字>」（新增 `getLastAgentText`），`.cg-log-toggle-summary` 单行 ellipsis；插件详情卡由既有 `renderCgResultCard(m.summary)` 承担。
- **版本**：index.html codegen.js bump r8。验证：node --check main.js+codegen.js、0 lint、静态断言 7/7（中文映射/可展开/完整input/markdown/init容错/折叠/summary）。需完全重启 Electron 生效。

## 三项优化：description 折叠 + 主页面显示最后回复 + 插件详情卡片（codegen.js r9）
用户跑 today-quote 扩展后反馈 3 项：
1. **工具参数有 `description` 字段时折叠显示 description**：`renderCgLogPartHtml` tool 分支新增 `toolInputPreview(it)`——JSON.parse 参数，若含非空 `description` 则优先展示（截断 160），否则截断 120 显示原始参数；同时 `<code>` 增加 `data-preview` 属性，`toggleCgLogToolDetail` 收起时恢复 preview（此前收起固定用 120 字截断，description 场景会丢失）。完整参数仍存 `data-full` 点击展开。
2. **主页面显示 AI 最后回复全文**：完成态新增 `renderCgFinalReply(m)`——取 `getLastAgentFullText`（最后一条 text part 完整文本 + `stripSummaryLine` 剥离末尾独立摘要 JSON 行）用 `renderMarkdownSafe` 渲染，显示「Agent 最后回复」气泡，位于结果卡片**上方**。样式 `.cg-final-reply`（透明底 + 复用 .cg-summary-md）。
3. **插件详情卡片未生成的根因与修复**：`parseAgentSummary` 之前只按 stdout 的**原始行**找摘要 JSON，但 agent 常把摘要 JSON **嵌套在 assistant 事件的 text 字段里**（JSON 转义后一行 `{"type":"assistant",...,"text":"...{\"type\":\"patch\",...}"}`），导致匹配不到 → `m.summary=null` → 走无摘要降级分支。修复：遍历行解析出 assistant 事件后，再在 `message.content` 的各 text 段里用 `findSummaryInTexts` 从后往前找摘要；`isSummaryObj`/`tryParseJson` 抽取为辅助函数。完成态逻辑合并：`m.ok` 时统一 `renderCgFinalReply + (summary? renderCgResultCard : 降级卡) + renderCgLogToggleStream`。
- **验证**：node --check + 0 lint；黑盒测试 PASS（构造嵌套 assistant text 的 stdout，parseAgentSummary 正确提取 `{type:patch,id:today-quote,...}`）。index.html bump r9。需完全重启 Electron 生效。

## 修复：最后回复被 CLI meta 文本污染（codegen.js r10）
- **现象**：r9 后主页面「Agent 最后回复」显示的是「CodeBuddy agent 执行完成。」而非 AI 真正的最后回复文本。
- **根因**：main.js `codebuddy:run` close 时发送 meta 文本「CodeBuddy agent 执行完成。」（带 `\n`），它和「启动 CodeBuddy CLI agent …」一样被 `parseAgentLogLine` 当成普通 `text` part 入组，且排在流末尾 → `getLastAgentFullText` 从后往前取到它。
- **修复**：新增 `isCliMetaText(s)`（匹配「启动 CodeBuddy CLI agent」「CodeBuddy agent 执行完成」「CodeBuddy agent 异常退出」前缀），`parseAgentLogLine` 最前面命中即归类为 `sys` 组（非 text，不作为 AI 回复候选）；`splitLegacyLogGroups` 旧数据回退路径同样处理。另加固 `getLastAgentFullText`：某 text part 剥离摘要后为空（如全是摘要 JSON）则继续向前找真正回复。
- **验证**：node --check + 0 lint；黑盒 6/6 PASS（meta 启动/完成识别、正常文本不误判、最后回复=AI正文、摘要 JSON 剥离、不含 meta）。index.html bump r10。需完全重启 Electron 生效。

## 发布 v0.2.0（后台发布成功，2026-08-04）
- **版本**：package.json + package-lock.json → 0.2.0；README 版本号 → v0.2.0；changelog 新增 id:17（v0.2.0：扩展生态里程碑 + AI 编程升级，涵盖 r6~r10 全部功能：多项目标签页消息流、agent 动态流式展示、扩展系统、CodeBuddy CLI 模式等）。
- **发布执行**：`npm.cmd run publish`（PowerShell 设 `GH_TOKEN`（读 User 级持久化环境变量 `[Environment]::GetEnvironmentVariable('GH_TOKEN','User')`，len=40）+ `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7892` + `CSC_IDENTITY_AUTO_DISCOVERY=false`），日志写 `publish_log_020.txt`。
- **本次无需补传**（对比 0.1.11/0.1.13 的经验）：80MB exe 一次上传成功，release v0.2.0 三资产齐全——`My-Study-Table-Setup-0.2.0.exe`(84,220,246B) + `.blockmap`(88KB) + `latest.yml`(356B)。release id=364689079。
- **验证**：本地与远端 latest.yml 完全一致（version/sha512/releaseDate 2026-08-04T07:40:40Z）；exe 下载 302 后 HTTP 200 + 完整字节数。electron-updater 更新链路可用。
- **关键经验更新**：GH_TOKEN 已持久化在 User 级环境变量（非临时），本次无需用户重新提供即可发布；但仍不写入任何记忆/明文文件。

## 扩展页四项修复：滚动/刷新/导入/拖拽（v0.2.0 后）
1. **扩展页无法上下滚动**：`#section-extensions > .card` 内部原来直接是 `#extensionsList` 无滚动容器（其他页面均有 `.card-scroll`）。修复：index.html 中用 `.card-scroll.ext-card-scroll` 包住列表/存储提示/状态。
2. **刷新不更新列表**：`renderExtensionsPanel` 只在 `ExtManager.list().length===0` 时才 `loadAll`（ext-manager.js 旧逻辑），刷新按钮只重渲染不重扫磁盘。修复：`ExtManager` 新增 `reload()`——先 unmount 全部 → 清空 `_registry` → `loadAll()` 重扫磁盘+内置 → 重装已启用扩展 → renderSidebarNav；`refreshExtensionsList` 改调 `reload()`。
3. **导入扩展按钮**：main.js 新增 IPC `ext:import`（`sourcePath` 为空时 `dialog.showOpenDialog` 选文件夹；校验：目录存在/含 manifest.json/`safeExtId` 与原始 id 完全一致防路径穿越/目标不存在；用 `fs.cpSync` 整目录复制到 `extensionsDir/<id>`，失败清理残留）。preload 暴露 `extImport`。settings.js 新增 `importExtension()`。index.html 工具栏加「导入扩展」按钮（folder-input 图标）。
4. **拖拽文件夹导入**：index.html 加 `.ext-drop-zone` 拖放区；preload 暴露 `webUtils.getPathForFile(file)`（Electron 官方方案，contextBridge 传 File 可行）；settings.js `setupExtDropZone()` 注册 dragenter/dragover/dragleave/drop，drop 时逐文件取路径调 `extImport`，统计成功数并 reload+刷新。`renderExtensionsPanel` 开头调 `setupExtDropZone`（data-ready 幂等）。
- **验证**：main.js/preload.js/settings.js/ext-manager.js `node --check` + 0 lint；黑盒 5/5 PASS（合法导入/重复拒绝/缺 manifest 拒绝/非法 id 拒绝/文件拒绝）。**关键安全点**：manifest.id 用 `safeExtId` 净化后须与原始值完全相等，否则拒绝（防止 `../` 路径穿越）。
- **注意**：改动了主进程 main.js + preload.js，须**完全重启 Electron** 生效。
- **微调**：用户要求不要虚线拖放区限制，改为整个扩展页面任意位置都可拖拽导入。`setupExtDropZone` 改为监听 `#section-extensions`（dragenter/dragover 加 `.dragging`，dragleave 仅 `e.target===sec` 时移除避免子元素闪烁，drop 时取路径导入）；index.html 删除 `.ext-drop-zone` 虚线区；CSS 改为 `#section-extensions.dragging` 虚线轮廓 + 居中「松开以导入扩展」提示气泡（`position:relative` + `::after`）。

## 卸载扩展：自定义确认弹窗 + 移入系统回收站
- **需求**：(1) 卸载二次确认改为 `showCustomConfirm` 形式（与 AI 对话标签页删除一致，非原生 confirm）；(2) 卸载后的扩展放入回收站。
- **渲染层（settings.js removeExt）**：改用 `showCustomConfirm(msg)`（HTML 消息 + 小字说明），内置扩展提示可恢复、非内置提示「扩展目录将移入系统回收站，可从回收站恢复；扩展数据 study_ext_<id>_ 前缀保留」。确认后状态栏区分「已移除/已卸载，已移入回收站/已卸载」。
- **主进程（main.js ext:remove）**：改为 `await shell.trashItem(dir)` 移入系统回收站（可恢复）；`trashItem` 失败（回收站禁用/网络盘）时回退 `fs.rmSync` 永久删除，返回 `{ok:true, trashed:boolean, reason?}`。
- **ExtManager.remove**：透传 IPC 结果返回 `{ok:true, trashed}`；内置扩展仍走 localStorage 标记移除（不删文件）。
- **验证**：3 文件 node --check + 0 lint。改动涉及主进程，须完全重启 Electron 生效。
- **修正**：用户明确要求「不是系统回收站，而是软件内回收站」。推翻 shell.trashItem 方案，改为扩展内置回收站：
  - **位置**：`extensions/trash/` 目录（卸载时 `fs.renameSync` 移动，失败回退 cpSync+rm；目录名 `<id>-<base36 时间戳>`，重名加随机后缀）。
  - **IPC（main.js）**：`ext:remove`（移入 trash）、`ext:trash-list`（扫描 trash 子目录，解析 manifest + 删除时间 base36→Date，按时间倒序）、`ext:trash-restore`（校验 id + `isPathInside` + 目标不存在，rename 回 extensions/<id>）、`ext:trash-purge`（永久删除）。目录名正则 `^[a-zA-Z0-9_-]+$` + `isPathInside` 双重防路径穿越。
  - **preload**：新增 `extTrashList/extTrashRestore/extTrashPurge`。
  - **渲染层（settings.js）**：`renderExtensionsPanel` 加载 `extTrashList`，在「已移除的内置扩展」下方渲染「回收站（N）」区块（虚线卡片：名称/id/版本/删除时间 + 恢复/彻底删除按钮）；`restoreTrashedExt`（恢复后 ExtManager.reload + 刷新）、`purgeTrashedExt`（`showCustomConfirm` 二次确认不可恢复）；`removeExt` 文案改「移入本页下方的回收站」。CSS 新增 `.ext-trash-list/.ext-trash-item/.ext-trash-info/.ext-trash-actions`。
  - **验证**：3 文件 node --check + 0 lint；黑盒 8/8 PASS（卸载入回收站/列表/恢复/重复恢复被拒/再卸载/彻底删除/purge 与 restore 路径穿越被拒）。改动涉及主进程，须完全重启 Electron 生效。
  - **再修正**：用户指出「本来软件中就有回收站」——应用已有统一回收站页面（js/trash.js + section-trash，管理 todos/notes/links/habits 四类 localStorage 数据）。因此**撤销扩展页内回收站区块**，改为把扩展回收站**集成进统一回收站页面**：
    - `trash.js`：`renderTrash` 末尾调用新增 `renderTrashExtensions()`（async，`extTrashList` IPC 拉取扩展回收站，渲染为第 5 个模块区块「扩展」，紫色 badge，复用 `.trash-item` 样式，恢复/删除按钮分别调 `restoreTrashedExt`/`purgeTrashedExt`）；空态加 `id="trashEmptyState"` 供扩展区块加载后移除。`emptyTrash` 一并调用 `extTrashEmpty` 清空扩展回收站。
    - `main.js`：新增 `ext:trash-empty` IPC（清空 trash 目录全部内容）；`preload` 暴露 `extTrashEmpty`。
    - `settings.js`：移除扩展页内 `trashedExts` 加载与回收站区块渲染（还原空态判断）；`restoreTrashedExt`/`purgeTrashedExt` 保留为全局函数（回收站页面调用），操作后改调 `refreshActiveExtSections()`（回收站页刷新 renderTrash + 扩展页刷新 renderExtensionsPanel）。
    - CSS：删 `.ext-trash-*`，新增 `.trash-item-type-badge.ext`。
    - index.html：trash.js bump r3、settings.js bump r6。
  - **验证**：4 文件 node --check + 0 lint。改动涉及主进程，须完全重启 Electron 生效。
- **Bug 修复**：`ext:list` 会把 `extensions/trash/` 回收站目录误扫为扩展（manifest 解析失败、id 回退为 `trash`）。修复：`ext:list` 的目录过滤追加 `e.name !== 'trash' && !e.name.startsWith('.')`（跳过回收站与隐藏目录）。node --check + 0 lint，须重启生效。

## 发布 v0.2.1（后台发布成功，2026-08-05 01:00）
- **版本**：package.json + package-lock.json → 0.2.1；README → v0.2.1；changelog 新增 id:18（扩展管理体验升级：软件内回收站、导入扩展按钮+全页拖拽、刷新重扫修复、trash 目录不误扫）。
- **发布执行**：同 v0.2.0 流程（PowerShell 设 GH_TOKEN(持久化 User 级) + 代理 7892 + CSC_IDENTITY_AUTO_DISCOVERY=false），日志 `publish_log_021.txt`。
- **资产**：release v0.2.1（id=364728552）三资产齐全——`My-Study-Table-Setup-0.2.1.exe`(84,224,677B) + `.blockmap`(87,976B) + `latest.yml`(356B)，一次上传成功无需补传。验证：exe 下载 HTTP 200 + 完整字节；latest.yml 版本 0.2.1/sha512 JKfAXL8d.../releaseDate 2026-08-04T08:52:20Z。

## 发布 v0.2.2（后台发布成功，2026-08-04）
- **版本**：package.json → 0.2.2；包含好友系统 + 玻璃适配 + 邮箱确认修复 + 404/Realtime 修复 + 7日回填等全部改动。commit `5bdfe03`。
- **发布执行**：PowerShell 代理 7892 + `npm.cmd run publish`，一次成功上传，无需补传。
- **资产**：release v0.2.2 三资产齐全——exe + blockmap + latest.yml。0.2.1 因用户误在 GitHub 手动建过 release（electron-builder 超 2h 拒绝覆盖），改号至 0.2.2 发布。

## v0.2.3 插件系统升级：AI 三模式 + 插件市场
- **版本**：package.json 0.2.2 → 0.2.3；changelog id=20。
- **AI 编程三模式**（js/codegen.js + main.js）：
  - 新增 `cgMode` 状态（'craft'|'plan'|'ask'，默认 craft），输入区新增模式选择器（Craft/Plan/Ask 三个 pil 按钮，`cgSetMode` 切换）。
  - `buildAgentPrompt(userReq, mode)`：注入模式专用 system instruction（plan=规划、ask=问答、craft=开发），各模式有不同权限约束与输出约定。plan 产出方案 JSON（type:plan），ask 不要求 JSON 输出。
  - `runAgent(req, mode)`：plan/ask 模式跳过预备份，mode 透传至 IPC；仅 craft 模式成功后 ExtManager.loadAll/init。
  - `cgGenerate`：用户消息 + assistant 消息加 `mode` 字段；`runAgent` 传 `cgMode`。
  - 消息渲染：user 消息 plan/ask 模式显示彩色模式标记；`isSummaryObj` 放宽接受 plan type。
  - `main.js codebuddy:run`：接受 mode 参数，plan/ask 模式 `--allowedTools Read`（禁写入），craft 模式 `--allowedTools Read Edit Write`。
  - 视图：`_cgPlaceholder()` 按模式返回不同占位文字；生成按钮文字按模式变化。
- **插件市场**（js/store.js 新建 + index.html + core.js + settings.js + supabase/schema.sql + css/style.css）：
  - 后端：Supabase 新增 `plugin_store_items`、`plugin_downloads`、`plugin_ratings` 三表 + RLS + avg_rating 触发器；Storage bucket `plugin-store` 说明。
  - 前端 store.js（window.Store）：Supabase 客户端、认证（signUp/signIn/signOut）、浏览/搜索（名称/标签）、下载安装（_unzip→extWrite→ExtManager.loadAll）、上传发布（extRead→_zip→storage.upload→insert 元数据）、评分、ZIP 导入（installFromZip）。
  - 页面：section-store + 导航项 store + ALL_NAV_ITEMS + switchTab + 顶部栏（搜索/标签过滤/登录/发布/导入按钮）+ 网格卡片（类型徽章/名称/描述/标签/下载按钮/评分）。
  - 设置：设置弹窗新增「插件市场」tab（settingsTabStore/settingsPanelStore），配置 Supabase URL+anonKey，测试连接、使用说明。renderStoreSettings/saveStoreSettings/testStoreConnection。
  - CSS：.store-*（页面/卡片/弹窗/Toast/模式）、.cg-mode-*（选择器/标记）全部样式。
- **修改文件**：js/codegen.js（+120行）、main.js（+7行）、js/store.js（新建 470行）、js/core.js（+3行）、js/settings.js（+32行）、index.html（+36行）、css/style.css（+107行）、supabase/schema.sql（+90行）、package.json（版本号）。所有文件 node --check + 0 lint 通过。需完全重启 Electron 生效。

## 修复：store.js IIFE 内 window.Store 未定义报错
- 根因：`window.Store = (function(){...})()` 还在执行中，内部 `Object.keys(api).forEach(k=>{window.Store[k]=api[k]})` 此时 window.Store 仍为 undefined。删除该冗余循环即可——IIFE 返回 api 后 window.Store 已正确赋值。

## Supabase 配置统一（好友 & 插件市场共享）
- **诉求**：好友系统和插件市场共用同一 Supabase 项目，不应分别配置两遍 URL+Key。
- **方案**：统一 localStorage 键为 `study_supabase_config`；设置页新增「Supabase 连接」tab（database 图标），替换原来各自的 URL/Key 输入框。
- **改动**：
  - `js/friends.js`：`FRIENDS_CFG_KEY` → `'study_supabase_config'`，`getFriendsConfig` 新增迁移：先读新键→旧键`study_friends_config`→store旧键`study_plugin_store_config`。
  - `js/store.js`：`CONFIG_KEY` → `'study_supabase_config'`，同向迁移；`renderStoreSettings` 简化为状态提示+使用说明（URL/Key 输入移到共享面板）；`saveStoreSettings` 留空（兼容入口）。
  - `index.html`：新增 `settingsPanelSupabase`（URL+Key+保存按钮+状态），保留原 Friends/Store panel 但清空冗余内容。
  - `js/settings.js`：`loadFriendsSettings`→`loadSupabaseSettings`（读取 `supabaseUrl`/`supabaseAnonKey` id），`saveFriendsSettings`→`saveSupabaseSettings`（保存后同时重置 Friends 和 Store 客户端）；`switchSettingsTab` 新增 supabase 分支。
- **验证**：4 文件 node --check + 0 lint。需完全重启 Electron 生效。

## 删除好友和插件市场设置 tab
- **背景**：好友和插件市场的 URL/Key 已统一到「Supabase 连接」tab，原来的独立 tab 不再需要。
- **index.html**：删除 `settingsTabFriends`、`settingsTabStore` 两个 tab 按钮，以及 `settingsPanelStore` 面板 div。
- **js/settings.js**：删除 `switchSettingsTab` 中 store tab 的 `renderStoreSettings()` 调用。
- `store.js` 的 `renderStoreSettings` 保留（含 null guard，面板不存在时安全 return）。

## 修复：插件市场 403（plugin_store_items REST API 拒绝访问）
- **排查过程**：RLS 已关仍 403 → SQL 直接查表能通（count 0）→ REST API 直调 401（但 profiles 表正常 200）→ 根因是 **PostgREST 不认识新建的表**：`create table` 后没有 `GRANT` 给 anon/authenticated 角色。
- **修复**：在 Supabase SQL Editor 执行 `GRANT ALL ON public.plugin_store_items, plugin_downloads, plugin_ratings TO anon, authenticated` + `NOTIFY pgrst, 'reload schema'`。
- **schema.sql 加固**：在插件市场建表+DROP POLICY+新建策略之后、触发器之前，新增三段 `grant all on public.plugin_store_items/plugin_downloads/plugin_ratings to anon, authenticated`，确保后续新项目执行 schema.sql 时一步到位。
- **Toast 看不清修复**：`.store-toast-success/error/info` CSS 改白字+纯色背景（绿/红/蓝），深色/浅色模式都高对比可读。

## 修复：插件市场「发布」找不到本地扩展
- **根因**：`store.js` 的 `listUploadableExtensions()` 过滤依赖 `e.type`、`e.builtin`、`e.hasManifest` 三个字段，但 `main.js` 的 `ext:list` IPC handler 从未返回这些字段（只返回了 `id/dir/manifest/hasMain/size/mtime/error`）。
- **修复**：`main.js` 的 `ext:list` 返回对象新增 `hasManifest: !!manifest`、`type: manifest?.type || 'plugin'`、`builtin: false`、`name`、`version`、`author` 字段，从 manifest 中提取。
- 需完全重启 Electron 生效。

## 修复：发布插件报「manifest.json 解析失败」
- **根因**：`store.js` 调用 `extRead({ id, filename: 'manifest.json' })` 传参名 `filename`，但 `main.js` 的 IPC handler 只解构 `{ id, file }`→`file` 始终 undefined→默认读 `main.js`→JSON.parse JS 代码报解析失败。
- **修复**：`main.js` `ext:read` 改为 `({ id, file, filename })` 取 `(file || filename)`，兼容两种写法。
- 需完全重启 Electron 生效。

## 插件市场：取消审核，直接上架
- 将 `store.js` 上传状态从 `status: 'pending'` 改为 `status: 'approved'`，上传后直接出现在市场。
- 提示文字同步更新：「等待审核通过」→「已发布到市场」，「上传后将进入审核状态」→「上传后将直接发布到市场」。

## 修复：发布插件 Storage RLS 报错（分两轮）
- **第一轮根因**：`plugin-store` Storage 桶未创建且无 RLS 上传/读取策略。修复：建桶 + `"Authenticated users can upload plugins"`（for insert）+ `"Anyone can read plugin files"`（for select）。
- **第二轮根因（真正解决）**：上传用 `upsert:true`，而 bucket 里已存在同名文件（`ai-quick-actions/1.0.0/ai-quick-actions.zip`）→ 走 INSERT…ON CONFLICT UPDATE，但策略只有 `for insert` 没有 `for update` → RLS 仍拒绝。修复：改为 `"Authenticated users can manage plugin files"` `for all`（using+with check 均 `bucket_id='plugin-store' and auth.role()='authenticated'`），并 drop 仪表盘自动生成的 `Insert/Select <ref>_0` 旧策略。
- **schema.sql 加固**：Storage 桶创建 + `for all` 管理策略 + select 公开读策略，注释注明 upsert 必须 for all。

## 插件市场账户 UI 重构
- **显示昵称/用户名而非邮箱**：新增 `getStoreDisplayName(user)`（查 profiles 表 nickname||username||email，缓存到 `user._displayName`）；`_updateAuthButton` 与上传 `author_name` 均改用展示名。
- **发布者展示名**：新增 `_enrichAuthorNames(list)`，`fetchPlugins` 拿到列表后按 `author_id` 批量查 profiles 的 nickname/username 覆盖 `author_name`（老数据存的是邮箱也能正确显示）。
- **点击账户不再直接退出**：登录后按钮显示昵称，点击弹 `showAccountMenu()`（头像首字母+昵称+邮箱+「退出登录」按钮，走 `doSignOut` 关闭弹窗后刷新）；未登录点击仍弹登录窗。
- `window.Store` 新增导出 `showAccountMenu`、`doSignOut`。

## 插件市场账户 UI 二次重构（参照好友系统 fr-auth 风格）
- **登录弹窗改为 tab 切换**：`showAuthModal` 用 `.store-auth-tabs`（登录/注册两个 tab）+ `_storeAuthForm(mode)` 渲染表单，复用好友系统的 `.fr-auth-form/.fr-auth-icon/.fr-auth-label/.fr-auth-input/.fr-auth-status` 样式与 `.btn-add` 主按钮，Enter 提交。
- **注册支持用户名/昵称**：`storeSignUp` 增加 username/nickname 参数写入 auth user_metadata（触发 handle_new_user 建 profiles）；`doRegister` 读取 `storeAuthUsername/storeAuthNickname` 输入框。
- **账户菜单**：`showAccountMenu` 用 `.btn-add` 退出按钮样式。
- **CSS**：新增 `.store-auth-tabs/.store-auth-tab`，移除原自创 `.store-auth-logo/.store-auth-field` 等。
- **登出后发布者仍显示邮箱的根因修复**：`profiles` 表 RLS `profiles_readable` 原为 `auth.uid() is not null`（仅登录可读），`_enrichAuthorNames` 登出后读不到昵称 → 改 `using (true)` 所有人可读（schema.sql 已更新，线上库需执行 SQL 生效）。

## showCustomConfirm 支持自定义弹窗（隐藏图标/按钮）
- `showCustomConfirm` 新增两个 options：`hideIcon`（隐藏 alert-triangle 警告图标，index.html 的 confirm-icon 加 id `confirmIcon`）和 `hideActions`（隐藏取消/确定按钮）；`hideActions` 时 Enter 键不再自动关闭弹窗（避免与表单自身提交逻辑冲突）。
- 插件市场登录弹窗（`showAuthModal`）和账户菜单（`showAccountMenu`）均传入 `{ hideIcon: true, hideActions: true }`，消除多余的惊叹号和取消/确定按钮。

## 插件市场发布入口迁移到扩展页面
- **弹窗高度自适应（二次修复）**：`.confirm-dialog` 改 flex column + `max-height: calc(100vh - 48px)`，`.confirm-msg` 加 `flex:1 1 auto; min-height:0; overflow-y:auto`（滚动下放到内容层）；`.confirm-overlay` 加 `overflow-y:auto; padding:24px 12px` 且 `.confirm-overlay.open .confirm-dialog { margin:auto }`（超高时遮罩整体可滚）；`.store-auth-modal` 自身加 `max-height:72vh; overflow-y:auto` 双保险。**关键：index.html 中 CSS/JS 引用带版本号（?v=xxx），修改后必须 bump 版本号否则 Electron 用缓存旧文件**——本次 css→r6、ai-utils→r1、settings→r9、store→r2。
- **删除插件市场顶部「发布」「导入」按钮**：`renderStore` 顶部栏只保留登录按钮。
- **扩展页面每张插件/补丁卡片加「发布」按钮**：`settings.js` 的 `renderExtensionsPanel` 在非内置扩展的卡片 actions 区新增 `publishBtn`（`onclick="window.Store.doUpload('id')"`，带 upload 图标）。
- **doUpload 兼容扩展页调用**：未登录先弹登录窗并提示；发布成功仅在 storeGrid 存在时刷新列表，扩展页发布后留在原页。

## 插件市场三处修复（第三次）
- **弹窗高度自适应（JS 兜底）**：纯 CSS max-height 在 Electron 中可能失效，改在 `showCustomConfirm` 打开后 `requestAnimationFrame` 里测量 `.confirm-dialog` 高度，超过 `window.innerHeight - 40` 则内联设置 `maxHeight` + `overflowY:auto`；cleanup 时恢复内联样式避免影响下次弹窗。
- **弹窗高度贴合内容（用户澄清：不是内容超高，而是弹窗比内容高上下留白）**：根因是 `.confirm-dialog` 的 flex column + `.confirm-msg` 的 `flex:1 1 auto` 把内容拉伸撑开。修复：`.confirm-dialog` 去掉 flex，改普通块 + `overflow-y:auto`；`.confirm-msg` 去掉 flex 拉伸；padding 28→20；`.store-auth-modal` 去掉 `max-height:72vh` 改为内容贴合，精简 fr-auth-form 内边距；JS 兜底只设 dialog maxHeight 不再给 msgEl 加 flex。
- **剩余留白进一步收紧**：`.confirm-dialog` padding 20→16/20/12，`.confirm-msg` margin-bottom 8→0；`.store-auth-tab` padding 11→8、tabs margin-bottom 4→2；`.store-auth-modal` 内 fr-auth-form padding 12/4/4→10/4/2、h3 mb 10→8、label margin 8→6、status min-height 18→16、btn-add margin-top 4。css→r9。
- **留白最终收紧（用户持续反馈）**：`.confirm-dialog` padding→10/18/8；`.store-auth-modal` 内 fr-auth-form padding→4/2/0、h3 mb→4、icon mb→0、label margin→4/0/2、status min-height→8、btn-add margin-top→0；tabs mb→0、tab padding→6。css→r10。
- **留白 root cause 补充**：真正大头是 `.fr-auth-form h3` 只覆盖了 margin-bottom，**h3 浏览器默认 margin-top 未清零**（约 16px）→ 顶部留白。修复 h3 `margin: 0 0 4px`。另 `.store-auth-modal` 加 `white-space:normal`（覆盖 `.confirm-msg` 的 pre-line 避免模板字符串换行产生空行）、fr-auth-form `line-height:1.3`、input padding 6/10、status min-height 0。css→r11。
- **间距回调平衡（用户反馈太紧）**：过度压缩导致 tab/图标/输入框/按钮间过紧。回调到适中值：fr-auth-form padding→6/4/2、line-height→1.4、h3 mb→8、icon mb→4、label margin→7/0/3、input padding→8/12、status min-height→8、btn-add margin-top→6/padding 9/14、tabs mb→6、tab padding→8。css→r12。**经验：留白与紧凑的平衡点，表单各元素间距（label↔input↔button）建议保持在 6~9px，tab 内边距 8px。**
- **发布成功 toast 反馈**：`_storeFeedback` 改为全局浮动 toast（`#storeToastPop`，动态创建挂 body，样式 `.store-toast-pop` 参考好友 `fr-toast`：fixed 顶部居中，success 绿/error 红/info 蓝），任何页面可见，3.2s 自动消失。原 storeStatus/extensionsStatus 内嵌方式废弃。
- **登录/注册成功 toast + 自动关闭弹窗**：`doLogin` 成功加 `_storeFeedback('登录成功，欢迎回来！','success')`，`doRegister` 成功加 `_storeFeedback('注册成功，欢迎加入！','success')`；两者原有 `setTimeout(closeCustomConfirm, 800/900)` 自动关闭逻辑保留。store→r5。

## 扩展页显示发布状态
- store.js 新增 `fetchUploadedExtIds()`：查 `plugin_store_items` 中 `status='approved'` 的 `ext_id` 集合（Set），已导出。
- settings.js `renderExtensionsPanel` 渲染前调用 `window.Store.fetchUploadedExtIds()`，非内置扩展卡片头部新增徽标：已发布（绿色 `.ext-badge-published` + check 图标）/ 未发布（灰色 `.ext-badge-unpublished`），title 提示"已发布/尚未发布到插件市场"。
- CSS 新增 `.ext-badge-published/.ext-badge-unpublished`。版本号：store→r6、settings→r10、css→r13。
- **发布完成后刷新扩展页**：`doUpload` 成功后，除刷新插件市场列表外，若 `renderExtensionsPanel` 存在且 `#extensionsList` 在 DOM，则 `setTimeout(renderExtensionsPanel, 100)` 刷新扩展页（更新"已发布"徽标）。store→r7。

## 弹窗感叹号默认隐藏 + store 弹窗去掉取消/确定
- `showCustomConfirm` 感叹号（`#confirmIcon` alert-triangle）改为**默认隐藏**，仅 `options.showIcon: true` 时显示（原 `hideIcon` 选项废弃，语义反转）。
- store 的**发布选择弹窗**（showUploadModal）和**评分弹窗**（showRateModal）补传 `hideActions: true`；登录/账户弹窗移除多余 `hideIcon: true`（默认已隐藏）。store 的 4 个自定义弹窗（登录/账户/发布/评分）均无取消/确定。
- 普通确认类弹窗（删除确认等）保留取消/确定按钮（操作必需），但感叹号同样默认隐藏。版本号：ai-utils→r4、store→r8。

## 评分去重 + 显示上次评分可修改
- store.js 新增 `getMyRating(pluginId)`：查 `plugin_ratings` 当前用户对插件的评分，未评返回 0。
- `showRateModal` 改为 async：打开时先查用户已有评分，高亮显示已评星星（`.store-star-btn.active` 橙色 + star fill），并提示「你已评 X 星，点击星星可修改」。
- 去重天然满足：`ratePlugin` 用 `upsert` + `onConflict:'plugin_id,user_id'`，同用户重复评分更新记录而非新增；数据库 `plugin_ratings` 有 `unique(plugin_id,user_id)` 约束。
- CSS 新增 `.store-star-btn.active { color:#f0a030 }`。版本号：store→r9、css→r14。
- **插件/补丁徽标与 tag 无背景色根因**：`.store-tag` 用了不存在的变量 `var(--bg-hover)`（应为 `--hover-bg`）；`.store-type-plugin` 用了不存在的 `var(--accent)`（主色变量是 `--primary`）；`.store-type-patch` 的 `var(--warning)` 也未定义但 fallback `#f0a030` 生效。修复：tag→`--hover-bg`、plugin→`--primary`、patch→直接 `#f0a030`。

## 修复：下载/导入插件后文件夹为空（manifest 解析失败）
- **根因**：`store.js` 的 `downloadPlugin`（上传下载）和 `_installFromZip`（ZIP 导入）调用 `extWrite({ id, filename, content })` 格式，但 `main.js` 的 `ext:write` IPC handler 只认 `{ id, files: { manifest, main } }`（`ext-manager.js`、`codegen.js` 都传 `files` 格式）。参数不匹配导致只 `mkdirSync` 建了文件夹、什么都没写入 → 插件文件夹存在但无内容 → 装载时 manifest 解析失败。
- **修复**：两处改为 `extWrite({ id, files: { manifest, main: mainRaw } })`（manifest 传已解析对象，main 传原始字符串）。store→r10。
- **教训**：`main.js` 的 IPC 参数约定以 `ext:write` 为准（`files` 对象），新代码调用前需对齐，勿自定义 `filename/content` 参数。

## 修复：下载量不更新
- **根因**：`downloadPlugin` 调用 `sb.rpc('increment', ...)`，但 schema.sql 从未定义 `increment` RPC 函数 → 调用失败被 catch 吞掉，下载量永远不增。且 `plugin_store_items` RLS 是"作者可管理自己的插件"，下载者（非作者）无法直接 UPDATE。
- **修复**：schema.sql 新增 `increment_downloads(target_plugin_id uuid)`（`security definer` + `set search_path = public` 绕过 RLS 安全自增）；store.js 改为 `sb.rpc('increment_downloads', { target_plugin_id: item.id })`。
- **需在 Supabase SQL Editor 手动执行函数定义**（线上库 schema 不会自动同步）。store→r11。

## 插件市场显示"已安装"状态
- `_renderPluginGrid` 渲染前调 `window.electronAPI.extList()` 取本地已装扩展 id 集合；`installedIds.has(p.ext_id)` 判断。
- 已安装的卡片：header 加绿色 `.store-installed-badge`（check 图标 + "已安装"），安装按钮变为禁用态（"已安装"灰底）。
- CSS 新增 `.store-installed-badge`（绿色胶囊，`margin-left:auto`）。版本号：store→r12、css→r15。

## 严重事故修复：index.html script 标签破坏导致初始化中断
- **症状**：侧边栏不显示、初始页变成待办页（而不是设置的主页）。
- **根因**：`store.js` 的 `<script src="...store.js?v=...r12">` **缺少闭合 `></script>`**（`<script src="...">` 被写成 `<script src="...">\n<script>`，即 `>` 丢失），导致整个内联初始化脚本块成为 store.js 的文本内容、无法解析，`setTimeout` 初始化（renderSidebarNav + switchTab 到设置首页）全部不执行，回退到默认待办页。
- **修复**：补上 `></script>`。
- **教训**：bump 版本号时用 replace 修改 script 行，必须保证整行格式完整；改完检查 `read_lints` + 用 read_file 复核相邻行。

## 已安装徽标改实心绿
- `.store-installed-badge` 从浅绿底深绿字（rgba(16,185,129,0.12)/#10b981）改为**实心绿底白字**（#10b981/#fff），更醒目。css→r16。

## 登录/注册文字统一 + 按钮文字保留
- 插件市场与好友系统登录标题统一为「登录账号」、注册标题统一为「注册新账号」，tab 文字同。**但按钮文字保持「登录」「注册」不变**（用户明确要求按键不要改）。
- store.js：表单 h3 标题/tab 改「登录账号/注册新账号」；登录按钮还原「登录」、注册按钮保持「注册」。friends.js：标题/tab 改，登录按钮还原「登录」。
- 版本号 bump：store→r4、friends→r9。

## AI 三模式（ask 问问题）体验修复（v0.2.4，2026-08-05）
- **背景**：codegen.js 的 Craft/Plan/Ask 三模式完整链路已随 v0.2.3 发布（模式选择器 UI `#codegenModeSelector`、`buildAgentPrompt(req,mode)` 按模式注入指令、main.js `codebuddy:run` 按 mode 设 `--allowedTools`（plan/ask→'Read' 只读、craft→'Read Edit Write'）、消息带 `mode` 字段）。但 ask/plan 体验有 bug。
- **修复 1**：`renderCgAssistantBody` 成功分支——ask/plan 模式（无 plugin/patch JSON 摘要）原误显示「扩展文件已写入」，现按 `m.mode` 分流：ask→「回答完成，仅读取源码参考，未修改任何文件」+「继续提问」按钮；plan→「方案已生成」；craft 保持原样。
- **修复 2**：`renderCgResultCard` 增加 plan 类型分支——`s.type==='plan'` 渲染方案卡（map 图标 + 「实现方案」徽标 + 方案说明），不再误标「安全插件」/「已写入扩展目录」；craft 分支不变。
- **修复 3**：cgMode 持久化——初始化从 `localStorage.study_cg_mode` 读取（非法值回退 craft），`cgSetMode` 写入保存。
- 版本号：codegen→r11（index.html）、settings→r11、package.json→0.2.4，changelog 新增 v0.2.4 记录。
