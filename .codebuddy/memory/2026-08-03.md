# 2026-08-03

## 自动记忆系统重设计（去重重构）完成

用户确认目标：应用内 AI 长期记忆系统（`js/memory.js`），核心痛点是**自动记忆重复内容多**。方向：重新设计自动记忆系统，ABC 三方案可切换 + 可配置兜底阈值 + 手动唤醒兜底按钮。

### 涉及文件
- `js/memory.js`（核心重构）：常量新增 `DEDUP_MODE_KEY/DEDUP_THRESHOLD_KEY/DEDUP_SIM_THRESHOLD(0.42)` 等；文本分析区重写（`normalizeText` 归一化、`diceSimilarity`、`textSimilarity`=Dice+包含关系，替代原 bigram+Jaccard）；`detectContradiction` 改为"主体有一定相似(≥0.3) + 明显极性翻转(差≥2)"才判矛盾；`upsertAutoFact` 重写为六阶段（精确匹配/方案A短路/启发式扫描/矛盾检测/同向合并/新建），新增 `createAutoFact`+`mergeIntoEntry`（合并语义：有效重复+0.3、同向+0.15、text 取长更新+aliases 记录历史、detail 增量拼接、mergeCount 计数）；新增去重引擎 `runLocalDedup`/`requestAIDedupSuggestions`/`mergeDuplicatedFacts`/`runManualDedup` + `getDedupMode`/`getDedupThreshold`；`extractMemoryFromConv` 注入已有记忆清单(方案A/B)并解析 `merge_id`；`parseMemoryTags` 支持 `id` 字段合并；`runDailyMemoryIntegration` 增加每日 AI 兜底(方案A/B 且 autoFacts≥阈值)；`formatMemoryForPrompt` 提取指引重写为三种动作(新增/带id合并/不提交)，autoFacts 显示行加 `[id]`；`loadAiMemory` 迁移补 aliases/mergeCount/_normKey。
- `index.html`：自动记忆区域顶部新增 `memoryDedupMode` 方案选择器(A/B/C)、`memoryDedupThreshold` 阈值输入(10-50)、"立即去重"按钮 `memoryDedupRunBtn`；`memory.js?v=20260803-r1`、`settings.js?v=20260803-r1`。
- `js/settings.js`：`renderMemoryPanel` 恢复方案/阈值控件初值 + autoFacts 卡片 meta 行"已合并 N 条"徽标；新增 `saveMemoryDedupMode`/`saveMemoryDedupThreshold`/`runManualDedupBtn`。

### 方案定义（localStorage `study_memory_dedup_mode`，默认 'B'）
- A 完全API：写入仅精确匹配，去重全交 AI（提取注入已有记忆要求 merge_id + 每日全量 AI 判定）
- B JS+AI兜底（默认）：写入纯 JS 六阶段，每日 autoFacts≥阈值(默认30，可设10-50)时 AI 兜底 merge 建议
- C 纯JS：写入纯 JS 六阶段，无 AI 兜底零成本

### 验证
- 编写临时 node 测试脚本验证 10 个用例（近义改写合并/不同语义不合并/否定矛盾/极性矛盾/同向不误判/包含关系/归一化幂等）全部通过后删除
- 三文件 lint 无错误
- 注意：修改 js/ 后需完全重启 Electron 应用生效；版本号未 bump（发布流程由用户控制，当前 package.json 0.1.12）

## 追加：自动记忆 → 手动记忆转化按钮
- `js/settings.js`：`renderMemoryPanel` 中 autoFacts 卡片 actions 区新增 📌"转为手动记忆"按钮（onclick=`memoryConvertToManual(id)`）；新增 `memoryConvertToManual` 函数（调 `addManualNote` 写入 manualNotes 保留 content/detail，再从 autoFacts 移除，保存+重渲染+状态提示）。
- `index.html`：settings.js 版本号 bump 至 `v=20260803-r2`。

## 追加：全局下拉栏主题适配
- `css/style.css`：主题变量块分别加 `color-scheme: light;` / `color-scheme: dark;`（让原生 select 下拉弹层/箭头/日期控件等随主题配色）；新增全局兜底规则 `select`（`background:var(--input-bg);color:var(--text);border:1px solid var(--border)`）、`select:focus`、`select option`（`background:var(--card)`）、`select option:hover`。已有容器规则（.input-row/.modal-field/.settings-field/.timer-manual-field/.ai-toolbar-key-select）和内联样式优先级更高不受影响，仅兜底未适配的 select。
- `index.html`：`autoFormRepeat` 背景 `var(--bg)`→`var(--input-bg)`（统一）；style.css 版本号 bump 至 `v=20260803-r1`。
- 排查结论：HTML/JS 中所有 select（linkTypeSelect/navHomeSelect/editTodoRepeat/editLinkType/manualRecGoal）均已在主题化容器内，之前"白底"主因是缺 color-scheme + option 无样式。

## 追加：外观系统修复 + effective 解析重构
- **Bug 修复（玻璃关闭残留）**：原 `applyCustomTheme` 仅玻璃开启时调 `applyGlassOpacity`，关闭后 17 个 CSS 变量 alpha 残留 → 半透明无模糊。新增 `resetGlassVariables()`（遍历 GLASS_VAR_DEFS.light+dark 变量名 removeProperty + 移除 --glass-blur），玻璃关闭分支调用。
- **--glass-blur 无条件写入** → 移入 if(glass) 分支。
- **effective 值重复三元** → 新增 `getEffectiveTheme(cfg)` 统一"预设优先、cfg 兜底"解析（accent/bgType/glass 全套），`applyCustomTheme` 与 `renderAppearancePanel` 共用。顺带修复两处隐藏不一致：①应用链 overlay 原来直接用 cfg.bgOverlay 忽略预设 → 改用 eff.bgOverlay；②渲染面板 glassOpacity 默认 0 vs 应用链 100 → 统一 100。删除死变量 isPresetBg/isPresetGlassBlur 等。
- **applyCustomTheme 残留 `base` 引用（ReferenceError 隐患）** → 改 eff.bgType/eff.bgVideo。
- **URL 转义**：新增 `cssUrl()`（转义引号/反斜杠/括号），`buildBgImageValue` 两处 url 构造改用。
- **视频背景**：关闭分支 `pause()` 后增加 `removeAttribute('src')` + `load()` 释放资源。
- "恢复默认"按钮 themeResetBtn 已存在（settings.js 3551），无需新增。
- `index.html`：settings.js 版本号 bump 至 `v=20260803-r3`。

## 追加：外观系统逻辑改进（建议 1/2/4/5 实施）
- **外观模块拆分**：外观区块（`// Custom Theme & Background System` 到 `Evening Daily Report` 前，约 880 行）从 `js/settings.js` 剪切为独立 `js/appearance.js`（用 node 临时脚本按注释锚点精确迁移后删除）。`index.html` 在 settings.js 前加载 `js/appearance.js?v=20260803-r1`；settings.js 版本 bump 至 `v=20260803-r4`。唯一区块外引用是 switchSettingsTab 对 renderAppearancePanel 的 typeof 运行时调用（安全）。
- **派生入口彻底化**：`getEffectiveTheme(cfg, forcedMode?)` 支持指定明暗模式解析；`buildBgImageValue` 从 `(cfg, mode, preset)` 改为接收 `eff` 纯函数（唯一调用点 applyCustomTheme）。
- **保存为预设 Bug 修复 + 双 mode**：原 handler 直接读 `cfg.accent` 等原始值（预设模式下过期），且只写 `[mode]` 一侧。改为用 `getEffectiveTheme(cfg, 'light'/'dark')` 分别构建 light/dark 双份预设数据，存的是界面实际显示值。
- **恢复默认确认框**：themeResetBtn 加 `showCustomConfirm`，提示仅重置自定义外观、明暗主题与自建预设不变。
- 验证：10 项 node 单元测试（双 mode 解析、buildBgImageValue 输出、custom/glass 预设）全部通过；appearance.js + settings.js lint 无错误。

## AI 聊天树状重构完成（形态 A 分支对话）

用户需求："全部重构，做成树状聊天结构"。范围仅 AI 聊天模块；形态 A（分支对话）为主，每个对话标签页独立成一棵树（多对话=森林），候选/重新生成用树分支实现，旧线性数据自动迁移。

### 核心数据模型（`js/ai-tree.js` 新建）
- `conv.tree` = 节点字典（`{id, role, content, parentId, children[]}`），root 为 `{role:'root'}`；`conv.activePath` = 根→当前节点路径；**`conv.messages` 保留为活跃路径的扁平缓存视图**（由树引擎 `recomputeMessages` 同步），外部读路径（memory.js 提取、标题生成、导出、日报上下文）零改动。
- 关键 API：`isTreeConv`/`ensureTree`/`migrateConvToTree`（旧线性+`_candidates`→树，候选展开为兄弟分支，活跃候选接活跃路径）/`appendMessage`（追加到当前路径末尾）/`createBranch`（任意节点下分叉并切换）/`switchBranch`（切活跃路径）/`recomputeMessages`/`resetConvTree`/`trimConvMessages`（线性尾部限长，有分叉不裁剪）/`branchInfoAt`/`siblingBranchCount`/`siblingNodeIds`/`lastUserNodeIdInActivePath`。

### 写路径统一改树 API（全部 `conv.messages.push` → `appendMessage(conv,msg)`）
- `js/ai-api.js`：`callAiApi` 3 处（Kimi 搜索 assistant/tool 结果/final）、`runToolCallLoop` 2 处（工具 assistant + system 结果）；`buildApiMessages` 删除整个 `_candidates` 展开 + skipUntilNextUser 逻辑（conv.messages 已是活跃路径扁平视图）。
- `js/ai-send.js`：`sendAiMessage` 用户消息/最终回复/call_ai 错误/主错误全改 append；`regenerateAiMessage(nodeId)` 重写为「切到父 user 节点 → runToolCallLoop append 自动形成新分支」；`navigateCandidateBranch(nodeId,delta)`（兄弟分支循环切换）替代旧 navigateCandidate；`adoptCandidate`/`unadoptCandidate` 保留占位；删除 `getActiveCandidate` 与 `_candidates` 数据模型注释。
- `js/ai-tools.js`：`executeCallAiAndPush` 改 append。
- `js/settings.js`：`executeAutomation` 3 处 + 晨/晚日报各 2 处改 append，日报 `slice(-30)` 改 `trimConvMessages(conv,30)`（并删掉多余 `}` 修复括号）；启动时对每个 conv 执行 `ensureTree` 自动迁移；`getDailyReportConv` 新对话 `initTreeOnConv`。

### 渲染（`js/ai-render.js`）
- `renderAiMessages`：删除 skipUntilUser + `_candidates` 展开，直接遍历 `activePathNodes`（每个节点带 nodeId）；assistant 节点若兄弟分支>1 显示「N 分支」切换分页（navigateCandidateBranch）；assistant 显示「换一条」（regenerateAiMessage(nodeId)）；user 节点分支>1 显示「🔀 该提问有 N 个候选回复」+ 切换按钮。
- 新增树形导航面板：工具栏「分支树」按钮（`toggleAiTreePanel`），浮层展示当前 conv 完整树（`renderAiTreePanel` 递归渲染，`switchToTreeBranch(nodeId)` 点击切换分支）。`renderAiChat` 重建 DOM 后恢复面板状态。

### 其他
- `js/ai-conv.js`：`createNewConv` 加 `initTreeOnConv`；`deleteConv`（最后一项）/`clearConvMessages` 改 `resetConvTree`；`exportConvLog` 增加树分支导出（user 节点 children>1 时导出各分支链，兼容旧 `_candidates`）。
- `js/ai-utils.js`：`safeSaveAiConvs` 清理分支保留 `tree`/`activePath` 字段。
- `index.html`：AI 脚本区 `ai-utils` 后插入 `js/ai-tree.js`；所有改动模块 bump 至 `v=20260803-r1`（settings.js `v=20260803-r5`）。
- `css/style.css`：`.ai-chat-layout` 加 `position:relative`；新增 `.ai-tree-panel` 系列（浮层/头部/节点/active 态）、`.ai-msg-cand-pager`、`.ai-msg-branch-label`。
- 验证：树引擎 22 项单测 + 24 项集成测 + 15 项 E2E（迁移/append 链/分叉/切换/trim/buildApiMessages 等价）全部通过后删除临时测试；所有改动文件 lint + `node --check` 无错误。
- 注意：需完全重启 Electron 应用生效。

## 追加：分支树界面改为浮窗形式（模仿复习浮窗）

用户需求："分支树界面做成浮窗形式（模仿复习笔记的浮窗）"。参照复习浮窗（`.review-float-overlay`/`.review-float`，body 级 fixed overlay + 右下角可拖拽浮窗）改造。

- `index.html`：在复习浮窗后新增 body 级树浮窗 DOM（`#aiTreeFloatOverlay` + `#aiTreeFloat` + `#aiTreeFloatHeader` + `#aiTreeFloatBody`），复用 `.review-float-overlay`/`.review-float-header`/`.review-float-title`/`.review-float-close` 类，标题用 `git-branch` lucide 图标，关闭按钮 `closeAiTreeFloat()`。删除了原 renderAiChat 动态注入的 `#aiTreePanel` 内联容器。
- `js/ai-render.js`：`toggleAiTreePanel`/`renderAiTreePanel` 改为操作 body 级浮窗；新增 `openAiTreeFloat`（默认右下角 24px、重置位置、lucide 重绘）与 `closeAiTreeFloat`；`renderAiChat` 重建后恢复浮窗显示状态；新增树浮窗拖拽逻辑（IIFE，仿 today.js 复习浮窗拖拽，仅 header 可拖，`position:fixed` + left/top 跟随）。`switchToTreeBranch` 切换分支后重渲染消息 + 重绘浮窗。
- `css/style.css`：删除 `.ai-tree-panel` 系列，新增 `.ai-tree-float`（fixed 右下角、460px、圆角 16px、阴影、max-height 70vh、z-index 2001）与 `.ai-tree-float-body`（滚动）；保留复用 `.ai-tree-row`/`.ai-tree-node`(active/user/assistant/system/root)/`.ai-tree-dot`/`.ai-tree-empty`，active 态用紫 `rgba(139,92,246,0.12)` 对齐复习主题。
- 版本：ai-render.js `v=20260803-r2`、style.css `v=20260803-r2`。
- 验证：lint 无新增错误（CSS 7 条 line-clamp warning 为预存）；`node --check` 通过。
- 注意：需完全重启 Electron 应用生效。

## 确认：AI 上下文构建与日志导出已适配树导航
- `buildApiMessages`（ai-api.js）：遍历 `conv.messages`（树引擎同步的活跃路径扁平视图），天然只含活跃分支的消息，分支切换后上下文自动跟随；无 `_candidates` 残留。验证：复杂树（分叉+嵌套交换）下上下文只含活跃分支、切换分支上下文随之变化、contextLimit 裁剪正常。
- `exportConvLog`（ai-conv.js）：遍历 `conv.messages` 导出活跃路径消息；user 节点 children>1 时用 `conv.tree[m.id]` 导出所有分支链（🔀 分支回复）。扁平视图节点即树节点对象（带 id），`conv.tree[m.id]` 可查。验证：分支数/活跃索引/导出行均正确。
- 额外记忆点：`conv.messages` 中的元素就是树节点对象本身（`recomputeMessages` 直接把 `activePath` 映射的节点放进去），因此读取方可用 `m.id` 反查 `conv.tree[m.id]`。

## 追加：分支树按"交换"聚合显示（一次问答=一个节点）

用户需求："分支树应该将一次用户的信息及 AI 的回复（包含工具调用和可能的多次 API 调用）视作一个节点"。**数据模型不变**（仍为消息级节点，保证消息流/上下文兼容），只改树导航浮窗的**渲染逻辑**——把「一次 user 提问 + AI 回复链（含工具调用、多次 API 调用）」折叠为一个交换节点。

- `js/ai-tree.js`：新增两个纯函数——`collectNestedUserIds(conv, userId)`（收集子树内所有嵌套 user 节点，前序父交换优先，DFS 遇 user 记下并继续下钻 children，修复了原先遇到 user 就 continue 导致嵌套 user 丢失的 bug）与 `collectExchangeInfo(conv, userId)`（沿活跃分支 first-child 链下钻取最后一条非空 assistant 作为回复，折叠工具链，返回 `{reply, activeChildId, branchCount}`）。
- `js/ai-render.js`：`renderAiTreePanel` 重写为交换聚合渲染——每个交换节点显示「👤 提问摘要」+ 下方缩进一行「🤖 回复摘要」（`collectExchangeInfo` 取最终 assistant）；工具链中间节点（assistant(tool)/system）不展示；嵌套交换递归展开缩进；分叉时在提问行右侧显示 🔀 分支徽标（点击调 `navigateCandidateBranch` 循环切换候选）；点击交换节点调 `switchToTreeBranch(activeChildId)` 切换到活跃回复链尾部。聚合逻辑收敛到 `ai-tree.js` 共享函数。
- `css/style.css`：新增 `.ai-tree-node.exchange`（交换行样式）、`.ai-tree-q`（提问摘要）、`.ai-tree-reply`（回复摘要，左侧紫色连接线缩进）、`.ai-tree-branch-badge`（🔀 分支徽标）。
- 版本：ai-tree.js `v=20260803-r2`、ai-render.js `v=20260803-r3`、style.css `v=20260803-r3`。
- 验证：15 项交换聚合 node 测试（工具链折叠/分叉 branchCount+活跃回复/嵌套交换顺序/未回复 reply=null）全部通过后删除临时文件；lint + `node --check` 无错误。
- 注意：需完全重启 Electron 应用生效。

## 追加：分支树改为「逐分支展开」（重新生成的候选各占一行）

用户反馈：发送 A → 发送 B → 重新生成 B 后，分支树显示 `a / b🔀2`（折叠成徽标），期望 `a / b / b'`（每个候选回复独立一行）。

- `js/ai-tree.js`：新增 `collectExchangeBranches(conv, userId)`——返回 user 下**所有**候选分支的 `[{branchStartId, reply, isActive}]`（每个分支独立，reply 为折叠工具链后的最终 assistant 内容）。保留 `collectExchangeInfo`（仍可用于其他地方）。
- `js/ai-render.js`：`renderAiTreePanel` 的 `renderExchange` 改为——user 行独立一行；每个候选分支用 `collectExchangeBranches` 各渲染一个 `.ai-tree-reply` 行（`🤖 分支 N 摘要`），活跃分支加 `active` 高亮且可点击 `switchToTreeBranch(branchStartId)` 切换；删除 `🔀 分支徽标` 折叠逻辑。
- `css/style.css`：`.ai-tree-reply` 加 `cursor:pointer`、hover、`active` 高亮（紫色底 + 左边框变 primary）；删除不再使用的 `.ai-tree-branch-badge`。
- 版本：ai-tree.js `v=20260803-r3`、ai-render.js `v=20260803-r4`、style.css `v=20260803-r4`。
- 验证：11 项逐分支 node 测试（单分支/重新生成 2 分支/工具链分支取最终值/嵌套交换不干扰）全部通过；lint + `node --check` 无错误。
- 注意：需完全重启 Electron 应用生效。

## 追加：分支树改为「完整树路径」渲染（父 user 在多个分支前重复出现）

用户需求升级：之前逐分支展开是 `👤 b / 🤖 分支1 / 🤖 分支2` 并列在同一个 b 下；用户要的是**每个候选分支从该 user 重新开始独立展开完整子树**：
```
👤 a → 🤖 回复A → 👤 b → 🤖 分支 1 回复B → 👤 c → 🤖 回复C
               → 👤 b → 🤖 分支 2 回复B'   （父 user b 在分支2 前重复出现）
```

- `js/ai-render.js`：`renderAiTreePanel` 的 `renderExchange` 重写为**路径递归**——`for` 每个候选分支时先渲染 user 行（分支>1 时父 user 自然重复），再渲染该分支回复行（工具链折叠：沿 first-child 下钻但**遇到 user 节点即停止**，修复了原先会下钻进嵌套 user 导致取错回复的 bug），再沿 first-child 找第一个嵌套 user 递归展开。顶层入口用 `collectNestedUserIds` 过滤出 root 直属 user。
- `js/ai-tree.js`：删除不再使用的 `collectExchangeInfo` 和 `collectExchangeBranches`（聚合逻辑已内联进 render 的路径递归，保持无死代码）。
- 版本：ai-tree.js `v=20260803-r4`、ai-render.js `v=20260803-r5`。
- 验证：21 项路径递归 node 测试（线性 4 行/分叉 6 行含重复 b/嵌套 c 8 行/工具链折叠+分叉）全部通过；lint + `node --check` 无错误。
- 注意：需完全重启 Electron 应用生效。

## 追加：交换组渲染（用户消息+AI回复=同一节点，选中联动；删除分支N文字）

用户需求：(1) `👤 提问` 和 `🤖 回复` 视作**同一个节点**，只能同时选中/取消；(2) 删除"分支 N"前缀文字。

- `js/ai-render.js`：`renderAiTreePanel` 改为**交换组渲染**——每次「user 提问 + AI 回复（含工具链折叠）」包裹在 `.ai-tree-exchange-group` 容器中作为一个整体节点：👤 行 + 🤖 行；点击组内任意位置 `switchToTreeBranch(replyNodeId)` 切换该分支；**groupActive 判定以回复链终点 `activeSet.has(replyNodeId)` 为准**（⚠️ 不能用 `activeSet.has(userId)`——同一 user 的多个候选分支共享该 user 节点，会误高亮所有分支组）。删除了"分支 N"前缀，直接显示回复摘要。顶层 `foldBranch` 提取为独立函数（沿 first-child 折叠工具链，遇 user 停止）。
- `css/style.css`：新增 `.ai-tree-exchange-group`（容器承担 hover/active：hover 背景、active 紫色底 + inset 左侧框线）；`.ai-tree-node.exchange` 设 `pointer-events:none` 去掉独立 hover/active（交给组容器）；`.ai-tree-reply` 同样 `pointer-events:none` + 缩进调整（user 行下方紧贴），active 时紫色高亮。
- 版本：ai-render.js `v=20260803-r6`、style.css `v=20260803-r5`。
- 验证：14 项交换组 node 测试（线性组/分叉 3 组无分支N/联动高亮——活跃链上 a 与 B2 都高亮、B1 不高亮/切回 B1 后 B1 高亮/嵌套 c 递归）全部通过；lint + `node --check` 无错误。
- 注意：需完全重启 Electron 应用生效。
- 【08-04 编辑消息功能见 2026-08-04.md】
