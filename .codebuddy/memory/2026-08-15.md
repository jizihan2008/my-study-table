# 2026-08-15

## Git 推送（提交 22466da）
用户要求"先推送"，已将累计的 13 个文件改动提交并推送到 origin/main（7199f4e..22466da）。

本次提交包含的内容（来自此前多次会话的累积改动）：
- **测验状态修复**（books-ai.js）：新增独立 `_bkQuizGenerating` 标志，不再用全局 `_bkAiBusy` 判断"测验生成中"，避免章节提问/讲解时测验页误显示"生成中"。
- **目录 UX 优化**（books.js）：`bkRenderToc` 重建前保存/恢复 `scrollTop`，`bkKbSelToggle` 改为局部更新 checkbox + 工具栏计数，不再重建整表丢滚动位置。
- **构建队列反馈**（books-kb.js）：已有构建进行中时入队排队并 `showMiniToast` 明确反馈，不再静默无反应。
- **摘要页 UI 整理**（books-ai.js）：伪代码/图片两个开关按钮合并，hint 统一放在开关组下方。
- **同步 LWW 修复**（sync.js）：上传 `user_data` 不再传客户端 `updated_at`，改用服务器时间 `now()`（upsert + select 回读），避免手机/电脑时钟偏差导致另一设备永不拉取。补传同样不传 updated_at。
- **笔记系统大改**（notes.js，+481 行）：新增 `_annotations` 批注字段、预览区选区右键 → AI 解释（新建 AI 对话标签页）、批注锚点反查源文本、编辑笔记不再重置复习周期（改右键手动）、"完成复习"仅对到期笔记显示。
- 附带文件：.codebuddy/memory/2026-08-14.md、diag-caption-test.mjs、diag-mindmap.mjs、diag-sync-logs.mjs 首次入库。

注意：应用运行约定是修改 js/css 后必须完全重启 Electron 才生效。
