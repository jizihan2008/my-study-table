# 2026-08-13 工作记忆

## 备份系统补全 + 手机端本地备份（commit b587038）

**推送完成**：commit `b587038`（4 文件 +251/-19）并推送，`c33cea3..b587038 main -> main`。新增 `js/backup-idb.js`。

### 背景
- 排查 AI 长期记忆不同步时发现：备份系统 `MIGRATION_KEYS` 只有 16 个 key，**AI 记忆、计时、任务线、习惯、教材、日历、统计、长期目标等核心数据从未被备份** → 用户 100 个备份全无 study_ai_memory，数据丢失无法恢复。
- 根因之一：memory.js 用 push 想加 `study_ai_memory` 进 MIGRATION_KEYS，但 memory.js（1308 行）先于 settings.js（1310 行）加载，push 时 MIGRATION_KEYS 未定义，从未生效。

### 本次改动
1. **MIGRATION_KEYS 补全 16→33 个**（settings.js）：纳入 study_timer_records、study_taskline_v1、study_habits(+v1)、study_books_v1、study_books_meta、study_calendar_events、study_stats、study_longterm_goals、study_quick_access、**study_ai_memory**、study_bk_explain_logs_v1、study_bk_quiz_state_v1、study_todo_completed_log、study_todos/study_notes（旧版）。备份/导出/导入（collectAllBackupData/exportAllData/importAllData 共用此列表）自动生效。
2. **删除 memory.js 无效的 MIGRATION_KEYS push 代码**（加载顺序 bug 的残留）。
3. **新建 js/backup-idb.js**（v1）：手机端 IndexedDB 本地备份存储，DB `mst-backup`/store `backups`/keyPath `time`。API：save/list/read/remove/count/cleanup(maxFiles)。可存含大 AI 对话的完整备份（解决 localStorage 5MB 限制 + 大 key 同步跳过）。
4. **手机端自动备份改造**（settings.js performAutoBackup）：非 Electron 优先存 IndexedDB + cleanup 清理旧备份，失败降级 localStorage。
5. **手机端「打开备份目录」**（openBackupDirectory）：列出 IndexedDB 备份（时间/大小），每份可「恢复」(`restoreIdbBackup`) /「删除」(`deleteIdbBackup`)。不再提示"仅 Electron 可用"。恢复会覆盖当前同名 localStorage 数据。
6. **手机端导出备份**（exportAllBackups 改 async）：优先导出 IndexedDB 备份（source:'idb'），降级 localStorage。
7. index.html 引入 backup-idb.js（settings.js 之前）；settings.js r20→r21、memory.js r2→r3。

### 手机端备份方案（用户选定）
用户从 D 改为 **A+定期自动备份到手机本地**：核心靠云同步（已有），手机端定期用 IndexedDB 存完整备份（模拟电脑文件备份），可恢复/删除/导出。

## 教材目录页解析（books-pdf.js r2-r8 + books.js r21）持续迭代

**核心功能**：从原书目录页（Contents）解析完整章节层级（替代不完整的书签 outline）。

**关键坑与修复**：
1. **目录页收集范围**：从含 "Contents"（含间隔式 `C O N T E N T S`）的页开始连续收集。**必须用 `tocStarted` 永久守卫**——目录区结束后（isBody 触发）`tocActive=false` 但 `tocStarted` 保持 true，否则**正文中出现的 "contents" 单词会重新激活收集**，误收集到书末 Index 页（如 Sipser 书 `2.2 PUSHDOWN AUTOMATA 121` 这种大写 Index 条目）。
2. **isBody 停止条件**：遇 `chapter \d+ /`（正文页眉，含斜杠）、间隔式 `p r e f a c e`、`acknowledg`、`^\s*copyright`、`^\s*selected bibliography`、`^\s*index` 停止。注意**不能把 "CONTENTS" 当停止信号**（目录续页也以 "CONTENTS"+页码开头），否则漏后续目录（如 Sipser Part Two 缺 3.2/4/5/6）。`isBody` 需 `tocTexts.length > 0` 守卫（避免误伤目录首页）。
3. **目录点线（dot leaders）清理**：条目 `标题 ....... 页码` 需截断到第一个 `. .`，否则标题残留点线。
4. **Part 层级支持**：识别 `Part One: 标题 页码`（英文序数/罗马数字/数字），树层级自适应：有 Part 时 `Part(0)→章(1)→节(2)→小节(3)`，无 Part 时 `章(0)→节(1)→小节(2)`。
5. **章节标号**：标题带标号（`1.1 Introduction`、`4.6.1 Properties...`），用 `withNum(num,title)`。
6. **过滤**：STOP_WORDS（of/and/the/Preface 等孤立词）+ hasRealTitle（章标题需 ≥3 字母词）。
7. **颗粒度选择**：`bkAskChapterGranularity` 支持 `source` 参数（'toc'/'outline'），层级按钮显示语义名（Part/章/节/小节），默认选中章层并展开预览。
8. **切分 bug**：AI 切分原用 `else if` 挂在 outline 分支的 else 上，目录/书签成功时会误触发 AI——改为独立 `if (!chapters && ...)`。

**真实代码测试方法**：Node 中 `(0,eval)(readFileSync('books-pdf.js'))` 加载真实全局函数（mock window/pdfjsLib/genId），调用 `parsePdfFile`/`parseContentsFromToc`/`splitChaptersAtLevel` 验证。比复制逻辑到测试脚本可靠（脚本可能和实际代码有差异）。

## 目录层级识别·缩进辅助（books-pdf.js r8→r9，2026-08-13）

**目标**：原 `parseContentsFromToc` 仅靠章节编号（1/1.1/1.1.1）判断层级，无编号条目不准确。用户要求**用目录原文的文本缩进（x 坐标）识别层级**。

**改动**：
1. **收集阶段保留原始 x 坐标**（parsePdfFile 内）：目录页按 y 分行时，每行前缀编码其**原始最小 x**（`\t<minX>\t文本`），不在收集阶段归一化（避免页脚/页码 x≈0 噪声污染整页基准）。原实现用 `(行x-页minX)/8` 会因页脚 x=0 导致全部深度被 clamp 到 5。
2. **解析阶段全局聚类**（parseContentsFromToc）：收集所有行 x → 去重排序 → 按间隙 ≥6px 分组，组序号即缩进深度 indent。章/节/小节缩进差（如 Sipser 138→153→186）正确聚类。
3. **缩进辅助判定**：用带编号条目校准 `chapterIndent`/`sectionIndent`（章的典型缩进、节的典型缩进）。**无编号条目**（如 Sipser 的 `Big-O and small-o notation`、`Formal definition of...`）若缩进比「节」深 → 归属当前节下作为更深小节（L3）；否则（与节平级，如 Summary/Problems）→ 归属当前章下作为章的子节点（L1）。

**验证（真实代码测试，两本 PDF）**：
- 概率论第10版（无 Part）：树正确，章→节→小节→无编号尾部，无回归。
- Sipser 第3版（有 Part）：**无编号小节正确缩进归位**——`7.1 Measuring Complexity` 下的 `Big-O and small-o notation` 等从原「与节平级」改为「节的 L3 子级」，这是本次核心改善。

**版本**：books-pdf.js `r9`（index.html `?v=20260813-r9`）。

**坑**：命令行传中文路径/中文用户目录在 PowerShell 会被 GBK 编码破坏（`纪子涵`→`绾子涵`）。绕开方式：脚本内硬编码绝对路径 + Node `-e` 用 `\u7eaa\u5b50\u6db5` 转义拷贝到无中文临时目录（`C:\temp_mst_test`）再运行，测完清理。

## 代码审查 + 全部修复（2026-08-13，code review 批量修复）

**审查范围**：全部 45 个 JS + main.js/preload.js/service-worker.js/index.html，发现 14 个问题（3 高危 XSS + 若干中危）。**已全部修复**，修改 11 个 JS + 2 个 HTML/SW，lint 通过。

### 核心改动（安全基础设施）
- **utils.js 新增 `escapeJs()`**：用于单引号 JS 字符串上下文（onclick），先转义反斜杠再转义引号/换行，修复 `escapeAttr` 的反斜杠绕过（`\');alert(1)//` → `\\'` 闭合注入）。**`escapeAttr` 强化**：转义 & < > "（仅用于 HTML 属性上下文）。
- **凡 `onclick="fn('${data}')"` 一律用 `escapeJs`，属性值用 `escapeAttr`**，二者职责分离。

### 修复清单
1. **settings.js 扩展列表 XSS**（高危）：ext.id（磁盘目录名）、manifest.type 拼入 onclick 未转义 → 改 `escapeJs(ext.id)` + type 白名单三元；automation 列表 convTitle/timeLabel/lastRun 补 `escapeHtml`。
2. **friends.js 头像 onerror 注入**（高危）：好友昵称首字符拼入 `onerror` 内联 JS → 移除 innerHTML 注入，fallback 字母改为独立 `<span>` + `escapeHtml`，onerror 仅隐藏 img。
3. **appearance.js 预设卡 XSS**（高危）：自定义预设名/背景 URL/颜色拼入 HTML/style → 新增模块级 `safeCssColor()`/`safeBgImageUrl()`/`safeCssAngle()`（颜色白名单正则 + URL 协议白名单），`buildBgImageValue` 与预设卡预览共用；`getCustomPresets` 增加对象形状校验。
4. **store.js / friends.js PostgREST 过滤串注入**：搜索词清洗 `replace(/[(),.*%]/g,'').slice(0,50)`。
5. **timer.js 记录 ID 冲突**：`genTimerRecordId` 改为 `_syncTimerRecordCounter()` 先取「当前时间戳」与「已存记录最大 id+1」较大者，防时钟回拨/会话复用撞 id。
6. **service-worker.js 预缓存版本漂移**：core.js 版本同步为 `20260812-r8`。
7. **index.html**：`lucide@latest` 锁定 `0.469.0`（供应链+离线）；修复 L1294-1344 脚本缩进混乱；**全部修改文件版本号 bump 到 `20260813-r1`**（utils/settings/appearance/todos/links/books/books-ai/inbox/friends/store/timer.js）。
8. **inbox.js**：mailKey 加账号维度（`acc.user@host|uid`，防同 host 多账号 UID 撞 key）；previewImage 校验 dataUrl 仅位图格式。
9. **links.js/todos.js/books-ai.js**：onclick 上下文 `escapeAttr`→`escapeJs` 迁移；todos 状态色 `statusBgColor`/选项色加十六进制校验。

**经验**：`settings.js`(1311) 先于 `utils.js`(1312) 加载，但所有调用在函数体运行时执行，`escapeJs` 可用——**工具函数只要不顶层调用，加载顺序无影响**。审查结论：AI 对话渲染（formatMarkdownBase 先转义再解析）、主进程 web:read 协议校验、codegen spawn 数组传参等已有防护合格。
