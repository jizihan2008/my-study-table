# 2026-08-09

## 修复：AI 编程页用户消息模式徽标被拉伸成气泡同宽（css/style.css）
用户消息徽标 `.cg-mode-badge` 位于 `.cg-msg-body`（flex column）内，默认 `align-items:stretch` 会把徽标横向拉伸到与气泡同宽（纯调 padding 无效）。修复：给 `.cg-mode-badge` 加 `align-self: flex-start`，使方框贴合文字宽度（padding 1px 4px）。仅 css 改动，需完全重启 Electron 生效。

## 修复：AI 编程页模式标签统一中文（js/codegen.js）
模式选择器按钮由英文 Craft/Plan/Ask/Clarify 改为中文「开发/方案/问答/澄清」（均 2 字等长）；澄清卡与「澄清回答已记录」卡头部的 `cg-mode-badge-clarify` 徽标 `Clarify`→`澄清`（replace_all 2 处）；引导卡片 CG_GUIDE_ITEMS 标题 `Clarify 澄清`→`需求澄清`、desc 内 `Clarify 模式`→`澄清模式`；`_modeLabel`（发给 Agent 的任务模式）由「规划（Plan）」等中英混写改为纯中文「方案/问答/澄清/开发」。`cgModeBadgeLabel`（用户消息徽标）本就是中文 2 字，未动；craft 模式用户消息仍不显示徽标。index.html codegen.js 版本号未 bump（仅文案改动）。注意：改 js 后需完全重启 Electron。

## 新增：计时器右下角浮窗（2026-08-09，timer.js r20）
计时器开始后在右下角出现浮窗（参考音乐悬浮球 `music-float`），**只有点击 ✕ 关闭按钮才关闭**（暂停/停止/保存/重置均不自动关）。改动：
- `js/timer.js`：新增浮窗模块（文件末尾）——状态 `timerFloatVisible`；`createTimerFloat()`（`#timerFloat`，结构：`.tf-header` 可拖动 + `.tf-body` 点击跳 timer 页 + `.tf-time` 大号等宽时间 + `.tf-controls`）；`updateTimerFloat()`（创建/更新目标行与按钮、同步 `formatTimerTime(totalMs)`；若音乐浮球正显示则 `bottom:96px` 错开）；`closeTimerFloat()`；`timerFloatGo()`=switchTab('timer')；`makeTimerFloatDraggable()`（自建拖动，关闭按钮 mousedown 不触发拖动）。
- 接入点：`timerStart()` 末尾置 `timerFloatVisible=true`+update；`loadAndRestoreTimerState()` running 分支恢复时同样显示；`renderTimer()` 末尾调用 `updateTimerFloat()`（心跳/操作自动同步）。
- 按钮（只显示符号）：未运行=▶开始/💾保存/⟲重置；运行中=⏸暂停/⏹停止并保存，直接复用 timerStart/timerSave/timerReset/timerPause/timerStop。
- `css/style.css`：`.timer-float`/`.tf-header`/`.tf-icon`/`.tf-title`/`.tf-close`/`.tf-body`/`.tf-target-row`/`.tf-time`/`.tf-controls`/`.tf-btn`（z-index 9998，复用 mfSlideUp 动画，复用 `--primary`/`--danger` 圆按钮）。
- `index.html`：timer.js 版本 `20260809-r20`。注意：改 js 后需完全重启 Electron。

## 核实：「任务线 DAG 多线改造」计划已全部落地（勿重复实现）
用户会话自带 v0.2.6 计划（AI 锚点摘要/DAG 指导段/tlLayoutGraph extQuests/占位节点/tlGotoExternalQuest/ai-tools 描述/CSS/版本号），逐项核实后确认**全部已实现**且代码已迭代到 0.2.20（index.html style.css `20260808-r30`、taskline.js `20260808-r25`）。具体落点：buildAiSummary 锚点 `slice(0,6)`+DAG 指导段（taskline.js 491-542）；tlLayoutGraph/tlLayoutGraphManual 均支持 extQuests、`_ext` 深度 0 不递归（596/643-752）；tlRenderGraph 收集跨章节 ext+占位节点+虚线边+data-qid（811-907）；tlGotoExternalQuest 跳转高亮（1859-1873）；ai-tools.js quest_create 152-153/quest_update 156-157；CSS 6777-6788+6745+6811。结论：不要因旧计划重复实现，后续功能继续在当前版本上演进。

## 修复：任务线右键菜单点击外部不隐藏（js/taskline.js）
任务级菜单 `tlQuestContextMenu` 原先**没有任何全局关闭监听**（只有图级 `tlTaskContextMenu` 有 click 监听），导致左键/右键点击其它区域菜单残留。修复：新增 `tlCloseAllContextMenus()`（同时关闭图级+任务级），`document` 上同时挂 `click` 与 `contextmenu` 监听（排除两个菜单自身，且不 preventDefault 以保留默认右键行为）；并在 `tlShowGraphContextMenu`/`tlShowQuestContextMenu` 开头互斥关闭对方菜单（两个菜单不会同时显示）。注意：菜单自身打开时已 `stopPropagation`，不会误触 document 监听。

## 增强：任务图画布平移更自由（js/taskline.js）
`tlGraphCanvasDown` 平移逻辑重构：(1) 删除「内容不超视口就不平移」的早期 return（`if (iw<=cw && ih<=ch) return`），小图也可拖拽；(2) 平移边界从「内容一角必须留在视口内」（clamp 到 `[cw-iw, 0]`）放宽为「内容可完全拖出视口任意一侧，仅保留 `TL_PAN_MARGIN=150px` 余量避免找不到」；(3) 新增双击空白处复位视图（`ev.detail>=2` 时 inner 归零）；(4) 命中 `.tl-node`/`.tl-graph-legend-float`/`.tl-locked-banner-float` 不平移。注意：`cw/ch/iw/ih` 在函数开头仍须读取（onMove 闭包依赖）。

## 新增：任务图画布滚轮缩放 + R 键复位（2026-08-09，taskline.js r26）
- **滚轮缩放**：canvas 内联 `onwheel="tlGraphCanvasWheel(event)"`，`transform:scale()` 作用于 `.tl-graph-inner`（origin 0 0），指数平滑 `factor=exp(-deltaY*0.0015)`，范围 `TL_SCALE_MIN=0.4~TL_SCALE_MAX=3`，**锚定鼠标位置**（`left=mx-(mx-left)*k`）。
- **视图状态**：`tlGraphView={scale,left,top}` 统一管理缩放+平移；`tlClampGraphView`（边界按 `iw*scale` 钳制，保留 TL_PAN_MARGIN）；`tlApplyGraphView` 在 `renderTaskLine` 重绘后恢复（拖拽节点/切换章节后缩放平移不丢）；`tlResetGraphView` 复位 100%+归零。
- **复位快捷键**：原**双击空白处复位已移除**，改为按 **R 键**复位（document keydown，仅 section-taskline active 且有 `#tlGraphWrap` 时生效，输入框聚焦时不触发）。
- **缩放坐标适配**：`tlNodeDragStart` 屏幕像素差除以 scale 再写 pos；`tlShowGraphContextMenu` 右键定位除以 scale；`tlGraphCanvasDown` 命中 `.tl-zoom-indicator` 不平移。
- **UI**：左下角浮动缩放指示器 `.tl-zoom-indicator`（id=tlZoomIndicator，实时百分比，点击=复位，title 提示 R 快捷键）。index.html taskline.js 版本 `20260809-r26`。

## 修复：codegen.js 运行时 ReferenceError: fillCgSample is not defined
`js/codegen.js` 暴露段残留一行 `window.fillCgSample = fillCgSample;`（约 1910 行），但整个文件及 HTML/main.js 均无 `fillCgSample` 函数定义与调用（重构后遗留的死引用），导致加载即抛 ReferenceError。修复：删除该行暴露语句。同时批量核验了 codegen.js 其余 31 个 `window.*` 暴露符号均有对应 function 定义，无其它死引用。

## 修复：craft 完成后扩展信息卡片消失（js/codegen.js 回归）
v0.2.4 引入「AI 三模式」时把 `renderCgAssistantBody` 的结果卡条件从 `m.summary ? renderCgResultCard(m)` 误改为 `m.summary && m.summary.type === 'plan'`，导致 craft 模式（plugin/patch 摘要）只显示通用"Agent 执行完成"文案，`renderCgResultCard` 的 plugin/patch 分支（扩展名+badge+id+功能说明+已写入目录）成为死代码。修复：恢复为 `m.summary ? renderCgResultCard(m)`（内部已区分 plan 方案卡 / plugin|patch 扩展卡）。`parseAgentSummary` 严格校验 type∈{plugin,patch,plan} 且含 id+name，ask/clarify 只读模式不会误渲染。注意：js/ 改动后需完全重启 Electron 生效。

## 浮窗 UI 重排：时间左置跨两行 + 目标只显示名称（2026-08-09，timer.js r21）
右下角计时浮窗布局改为「时间在左、关联目标在右」：`.tf-body` 改 flex row，`.tf-time` 加大 30px、`align-self:stretch` 占据左列整列（跨关联任务/目标两行高度）、右侧 1px 分隔线；新增 `.tf-targets`（右侧 flex column，上下两行=关联任务+关联目标）。**关联任务只显示名称，不再显示目录路径**（`timerFloatTargetHtml` 删除 getAncestorPath 拼接）。**按钮符号与主计时器面板统一**：暂停/停止/保存改用 Lucide 图标（pause-circle/stop-circle/save，`updateTimerFloat` 末尾加 `lucide.createIcons()`），开始▶/重置⟲ 保留字符。index.html timer.js 版本 `20260809-r21`。
