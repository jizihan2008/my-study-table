# 2026-08-05

## 好友系统（Supabase 后端）v0.3.0
新增联网好友系统，一期完整实现（账号+好友+分组+动态+聊天），版本从 0.2.1 → 0.3.0。

### 数据库
- `supabase/schema.sql`：profiles / friend_groups / friendships / study_stats / activities / conversations / messages 共 7 表，全部 RLS（`is_friend()` 辅助函数），注册触发器 `handle_new_user`（从 raw_user_meta_data 建 profile），最后 `alter publication supabase_realtime add table ...` 开启 Realtime。
- 使用前提：在 Supabase 控制台执行该脚本；Auth → Email 建议关闭 "Confirm email"（否则注册后需邮件确认）。

### 前端
- `js/friends.js`（新）：配置(get/saveFriendsConfig, key `study_friends_config`)、Supabase 客户端单例(getSupabaseClient, window.supabase UMD)、认证(register/login/logout/getSession/getMyProfile)、分组(create/rename/delete/assign)、好友(search/send/respond/remove)、聚合统计(computeDailyStats/syncStudyStats)、动态流(loadActivities/subscribeActivities)、在线心跳(60s 更新 profiles.last_seen)、UI 渲染(renderFriends/setup/login/home 三态)。
- `js/friends-chat.js`（新）：会话(getOrCreateConversation, 规范化 user_a<user_b)、消息(load/send/markRead)、Realtime 订阅、未读数、聊天 UI、好友资料卡(7日聚合统计)、删除好友。
- `index.html`：新增 `section-friends`（card padding:0, #friendsApp）、侧边栏 nav、脚本 supabase.js + friends.js + friends-chat.js、设置页新增「好友」tab(settingsPanelFriends)。
- `js/core.js`：ALL_NAV_ITEMS 新增 `{id:'friends',icon:'users',label:'好友'}`；switchTab 新增 friends 分支。
- `js/settings.js`：loadFriendsSettings/saveFriendsSettings；changelog 新增 id:19 (v0.3.0)；switchSettingsTab/openSettingsModal 挂 friends 分支。
- `css/style.css`：新增好友系统全部样式（fr-* 前缀）。

### 补充（同日迭代）
- **注册页**：新增「确认密码」输入框 + 一致性校验；用户名格式校验（中文/字母/数字/_/-，1~30）；注册成功弹出顶部 Toast `friendsShowToast('🎉 注册成功，欢迎加入！')`（`.fr-toast` 样式，支持 error 类型）。
- **设置页「保存并重新连接」反馈**：`saveFriendsSettings` 改为 async——按钮禁用+「⏳ 正在连接…」、真实测试连接（`client.from('profiles').select('id',{count:'exact',head:true})`）、智能报错（42P01→未建表 / 42501→无权限 / JWT→key 无效 / 网络异常），成功绿色 / 失败红色，按钮恢复并重绘 Lucide。
- **GoTrueClient 多实例警告根治**：`initFriendsAuth` 加 `_friendsAuthInit` 防重 + 保存 `_friendsAuthUnsub` 订阅；`resetSupabaseClient` 先 unsubscribe auth 再置空客户端，避免新旧实例共存。
- **邮箱确认回调页**：`main.js` 新增 `startConfirmServer()`——app ready 时用 `http` 模块监听 `127.0.0.1:3000`，返回友好确认页（`CONFIRM_PAGE_HTML`，JS 读 location.hash 判断 type=signup/recovery/email_change/error 显示不同文案），端口占用则静默跳过；`before-quit` 时关闭。**注意**：Supabase 确认链接默认跳 localhost:3000，此服务器让浏览器能打开"注册成功"页而非报错；但仍推荐在 Supabase Authentication→Providers→Email 关闭 Confirm email 一步到位。

### 好友可见内容扩展（用户需求：标题级动态）
- **新表 `weekly_focus_todos`**（schema.sql）：user_id / week_start(周一) / todo_id / title / focus_ms，`unique(user_id, week_start, todo_id)`，RLS 仅本人+好友可读，已加 GRANT + realtime。**需重新执行 schema.sql 建表**。
- **新动态类型**（syncStudyStats 扩展，去重 key 复合 `todo:<id>`/`note:<id>`）：
  - `todos_done`：今日完成待办逐条发布「完成待办「标题」🎯」（getTodayCompletedTodos 从 study_todos_v2 取 completedAt===today）
  - `note_edit`：今日编辑笔记发布「编辑笔记「标题」」+ meta.summary（前 60 字去 Markdown 语法，getTodayEditedNotes 从 study_notes_v2 按 updatedAt/createdAt 判今日）
- **资料卡「本周专注 Top5 待办」**：friends.js `computeWeeklyTopTodos()`（study_timer_records 中 date∈[周一,今天] 且 affectsFocus，targetType 非 todo 排除，从 todos 补标题，取 top5）→ upsert weekly_focus_todos；friends-chat.js `frShowFriendProfile` 查该表按 focus_ms 倒序显示（.fr-profile-top-* 样式）。
- 动态流渲染：`note_edit` 在 content 下追加 `.fr-feed-note-summary`（左竖线摘要）。
- 隐私变化：待办标题、笔记标题+摘要、计时 top5 标题现会上传云端，仅好友可见（RLS 保证）。

### 关键实现点/坑
- 加载顺序：supabase.js(UMD) → friends.js → friends-chat.js（在 updater.js 后、扩展系统前）。settings.js 在 friends.js 之前加载，但 getFriendsConfig 是运行时调用，无冲突。
- `renderFriendsFeed` 被 `renderFriendsFeedView` 取代，两处订阅回调里必须用 `renderFriendsFeedView` + 手动 innerHTML 更新。
- 隐私：只同步聚合统计（打卡/专注毫秒/完成数/习惯次数/连续天数），不上传具体待办/笔记内容；靠 RLS 保证仅本人+好友可见。
- 聚合数据来源：study_checkin、study_timer_records(affectsFocus!==false)、study_todos_v2+study_todos_trash(completedAt)、study_habits_v2(checkins)。
- `friendsOpenChat`/`friendsCloseChat` 等跨模块函数挂在全局（顶层 function 声明）。
- 本模块依赖 node_modules/@supabase/supabase-js（`npm.cmd install --no-save`），UMD 已拷到 lib/supabase/supabase.js（210KB），lib/**/* 已被 electron-builder 打包。
- 修改 js/css 后须完全重启 Electron 才生效；当前未实际连接真实 Supabase 项目测试（需要用户提供 url+key）。

## CodeBuddy CLI 能力调研（AI 编程澄清功能决策依据）
本机已装 `@tencent-ai/codebuddy-code`（`%APPDATA%\npm\node_modules\@tencent-ai\codebuddy-code`），入口 `bin\codebuddy`。**结论：CLI 自带提问能力但 `-p` 非交互模式不可用，ask/craft 不是 CLI 概念**：
- **提问能力**：CLI 有 `AskUserQuestion` 工具（模型可主动弹问题卡）、`ExitPlanMode`（plan 完成请求批准），但**仅服务于交互式终端 / ACP 协议客户端**（`--acp`，stdin/stdout ndJsonStream 双向通信）。官方文档明确：**`-p` 模式下 CLI 不会交互式追问**，一次性执行并退出；CHANGELOG 记录 print 模式提问类工具走 "ask→deny" 语义（无审批 UI 入口，自动拒绝）。
- **ask/craft/plan**：CLI **没有** `--mode ask/craft/plan` 参数。plan 是**权限模式** `--permission-mode plan`（只读分析出方案，Shift+Tab/Alt+M 切换）；ask 不是权限模式；craft 不存在。ask/craft 是 CodeBuddy IDE 对话框 UI 层概念，我们目前用 `--allowedTools`（ask/plan→'Read'）+ prompt 注入模拟，方向正确。
- **多轮/继续**：`-c/--continue` 恢复最近会话、`-r/--resume` 按 id 恢复、`--input-format stream-json` 支持 stdin 流式输入、`--replay-user-messages` 重放消息。理论上可用于多轮，但 AskUserQuestion 在 -p 下仍被 deny。
- **实现「AI 反问确认方向」的可行路线**：(A) prompt 层注入澄清约定 `{"type":"clarify","questions":[...]}` + 渲染层提问卡 + 回答后带上下文重跑（不动 CLI 协议，推荐）；(B) 改用 `--acp` 双向协议监听 AskUserQuestion 事件（最接近 CodeBuddy 原生体验，但需重写 main.js 通信层，改动大）。

## AI 编程澄清模式 clarify + 项目会话分层注入（v0.2.4 基础上实施）
按方案 A（prompt 层澄清协议）落地，澄清功能已实现。改动文件：`js/codegen.js`、`main.js`、`css/style.css`、`index.html`（codegen.js?v=20260805-r12）。
- **新增独立模式 `clarify`**（与 ask 并存，ask 是用户问 AI 答，不覆盖）：cgMode 白名单加 clarify；`_modeSystemInstruction` 强约束"信息不足必须提问/优先提问/2~5 个问题/带 options 候选"；`_modeOutputConvention` 要求末行输出 `{"type":"clarify","questions":[{"q","options"}]}`；模式按钮 + placeholder + 徽标（`cg-mode-badge-clarify` 绿色）；**main.js allowedTools 映射加 `clarify→'Read'`**（澄清轮强制只读，这是唯一碰 main.js 的地方）。
- **项目会话分层注入（方案 C）**：`buildProjectHistory(project)` + `buildHistoryPair` + `historyDecisionNode`；`buildAgentPrompt(userReq, mode, project)` 在 `## 用户需求` 前插 `## 项目对话历史` 段；`runAgent(userReq, mode, project)` 新增第 3 参自动取当前项目。分层规则：**近期 6 对 user+assistant 完整重放，早期只保留决策节点**（clarify 问答、plan 方案、plugin/patch 完成摘要）。plan 不提问直接出方案并作为决策节点注入；craft 无澄清指令保持直接执行。
- **澄清交互**：`extractClarify`/`findClarifyInTexts`（与 parseAgentSummary 同款双形态检测）；runAgent 在 clarify 模式优先提取返回 `{clarify}`；消息对象新增 `clarify:{questions,answered,answer}` 字段；`renderCgAssistantBody` 在 running 后加两分支（提问卡 `renderCgClarifyCard` / 已回答 `renderCgClarifyAnswered`）；`cgAnswerClarify(uid)` 收集答案、`cgFillClarify` 点候选选项填入、`cgProceedAfterClarify(uid, mode)` 切模式+构造续接需求自动再跑；clarify 提取失败时 ok 分支兜底显示"AI 未提出澄清问题"（避免误走 craft 完成卡）。
- **坑**：clarify 模式提取不到 JSON 时若走旧 `if(m.ok)` 第三分支会误显示"扩展已写入"，已把 isClarify 并入只读卡片分支修复。历史注入中未配对 assistant 的 user 消息不输出；runAgent clarify 分支提前 return 不触发扩展装载（正确）。
