# 2026-07-07 Daily Log

## 文件夹删除改为"移到上一层"
- 修改 `js/notes.js` 的 `deleteNote` 函数：文件夹删除时，子级不再移到根目录（`parentId=null`），而是移到被删除文件夹的 `parentId`（上一层）。
- 修改 `deleteNoteFolder` 确认对话框文字，明确提示"文件夹内的内容将移动到上一层"。

## 文件夹折叠功能修复 + 拖放优化
- 修复 `toggleNoteFolder`：图标（▾/▸）现在随折叠状态同步更新；收起时「+ 新建笔记」按钮也一起隐藏。
- 修复空白区域拖放：鼠标在列表最上方区域时，指示符显示在第一个元素之前，drop 后调用 `unshift` 插到开头；底部区域仍在末尾追加。

## list_notes 改为层级结构输出
- `list_notes` 工具：重写为与 `list_todos` 一致的递归编号格式（如[1]、[1.1]），用 📁 标注文件夹、📄 标注笔记。
- `add_note` / `move_note`：修复使用 `parentId` + `type:'note'` 替代旧字段 `folderId`。
- 数据快照笔记行显示「X 篇（Y 个文件夹）」
- 系统提示模块概览和规则3同步更新，说明笔记层级结构。

## 文件夹交互优化
- 删除双击文件夹重命名（`ondblclick` 移除）
- 新建文件夹不再弹窗输入名称，直接创建为「新建文件夹」
- 右键菜单动态显示：右键文件夹才显示「重命名/删除」，右键笔记/空白只显示新建
- 设置→笔记面板保留「重置文件夹」按钮

## 新增音乐播放器栏目
- 新增 `js/music.js` 模块：完整的音频播放器，支持导入/播放/暂停/上一首/下一首/进度控制/音量控制
- HTML 新增 `section-music` 和侧边栏导航项
- CSS 新增全套音乐播放器样式
- 注册到 `core.js` switchTab 和 `utils.js` 初始化
- Electron IPC 新增 `open-audio-dialog`（文件选择）和 `read-audio-file`（读取为 base64 Data URL）
- preload.js 暴露新 API

## 音乐播放悬浮球
- 当有音乐播放时，右下角出现可拖拽的悬浮球
- 显示当前歌曲名称、播放时间、进度条
- 悬浮球上点击播放/暂停按钮控制音乐
- 点击悬浮球主体区域切换到音乐栏目
- 无音乐时自动隐藏
- 支持拖拽移动位置

## 新增「维度大陆」游戏系统（第一阶段）
- 新增 `js/dimension.js` 模块：AP（行动点数）系统、补签功能、里程碑奖励、假日模式、腐化能量转换
- AP 获取：每日打卡 +3 AP，每 7/30/365 天里程碑额外奖励 AP+补签机会
- 补签功能：消耗补签机会维持连续打卡，不获得 AP
- 假日模式：设置中可开启，忘记打卡不会中断连续天数
- 腐化能量：隔日未使用的 AP 自动转化
- 侧边栏新增「维度大陆」导航项，紫色渐变主题头部
- CSS 新增全套维度大陆样式（AP 条、统计卡片、里程碑进度条等）
- 注册到 core.js switchTab、utils.js 初始化
- 设置面板调试栏目添加假日模式开关
- MIGRATION_KEYS 添加 study_dimension
